# ТЗ · чат A · шаг 4 ядро

Себе. `pullOneStudent` + тесты. Экран — чат D.

Не трогать: `admin-crm-settings.tsx`, B, C, F, шаг 5, `peopleJobFinished`, `PAY_INBOUND_*`, ромашка. Дефолт `pageCap` census шага 2 (=12) не менять.

Ключи: `customerId`, `branchId`, `lessonId`, id платежа, `cttId`. Не имя.
Журнал: набор `lessonId` + `diskRows === diskUniq`. Checksum не замок.

## API Alfa

- 5 запросов/с. Limiter в коде нет — не плодить.
- Урок живёт в `{branch}` URL. `lesson/index` без `status` → 3. Даты `YYYY-MM-DD`. `pageSize` 500.
- Пустые даты census → 2015 и +90. Шаг 4: оба конца, как `ymd(ruShift(±N))` шага 2.
- 4 филиала × 3 статуса = 12 запросов на страницы 0.
- **Не** «пропустить мёртвый филиал и prune». Урок в том филиале, prune снимет его с диска как «ушёл». Любой `!live.ok` → `census.ok=false`, prune нет. Касса уже записана. Как сейчас `if (!live.ok) return ok:false` — для шага 4 оставить.
- `inboundMissing` на шаге 4 не звать.
- `pay/index` без дат урока.

## Зачем

`if (!balance)` — уроков нет. `else` шага 2: missing + `windowAlfaKeep` → зелёные желтеют.

## Шаг 1. Синяя `balance && recheck`

Один `waitLockStudentAlfa(cid)`. Не взяли — кассу не начинать. Unlock в `finally`.

1. Касса первая, `{ force: true }`. Без дат урока.
2. Журнал только если `scanDone`.
3. Окно: `clampRecheckDays` + `ruShift(−N)/ruShift(+N)` → YYYY-MM-DD. Не `wasLessonGreen`.
4. Census с этими датами, `pageCap: 1` аргументом (дефолт 12 не трогать).
5. Только если `census.ok`: `applyCustomerLessonCensus(cid, ids, true, from, to)`.
6. Не вызывать: `inboundMissing`, `windowAlfaKeep`, stamp `lessonsAlfa` / `lessonsSeenIds` из окна, `mark()` журнала, `lessonsRecheckAt`.
7. `!census.ok` — календарь не трогать.

`shouldRetryOpenRecheck(balance)` только `!paysRechecked`.  
`paysAt` / `paysRecheckAt` — блок кассы после `scanDone`.

## Шаг 2. Красная

Только касса. Вправо `paysScanned || pays`.

## Критерий A

1. Pay до lesson.
2. Нет `scanDone` → lesson нет.
3. Окно не 2015, не +90.
4. Нет missing, нет роста `lessonsAlfa` от окна.
5. Тест: диск 100, окно +2 не посадили — short нет.
6. Тест: prune снял id в окне, старше окна остались.
7. Тест: один филиал `ok:false` — календарь без изменений.
8. Дефолт `pageCap` census = 12.
9. Экран без диффа.

Простыми словами: касса первая. Синяя снимает уроки, которых в окне ± месяц нет во **всех** ответивших филиалах Alfa. Если хоть один филиал молчит — календарь не трогаем. Новые не добираем, счёт не поднимаем.
