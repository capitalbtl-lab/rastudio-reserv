# ТЗ · чат A · шаг 4 ядро

Себе. `pullOneStudent` + тесты. Экран — чат D, не трогать.

Не трогать: `admin-crm-settings.tsx`, B, C, F (`pay.create` / poll), шаг 5, `peopleJobFinished`, ромашка. Деплой — по просьбе.

Ключи: `customerId`, `branchId`, `lessonId`, id платежа, `cttId`. Не имя.
Журнал закрывает набор `lessonId` + `diskRows === diskUniq`. Checksum не замок. Равный счёт, разные id — не готово (03f).

## Зачем

`if (!balance) { проба / окно ±N }`. Шаг 4 только `inboundCustomerPays`. Экран после D шлёт `recheckDays` и годы — ядро их ест впустую.

## Шаг 1. Синяя `balance && recheck`

Сначала тот же блок, что шаг 2 синяя (`else` сейчас): окно `recheckCensusWindow` ±N, перепись, prune в окне, missing 50, `keepAlfaProbe` / `windowAlfaKeep`, галка. Не 2015. Не полный fill. Нет даты — dropped.

Потом касса **всегда**, даже если журнал окна не закрылся: `inboundCustomerPays` `{ force: true }`, курсор, страницы. `pay/index` окном ±N не фильтровать.

Дырка журнала после синей — шаг 2, не держать очередь шага 4. `shouldRetryOpenRecheck(balance)` по-прежнему только `!paysRechecked`. Не добавлять `!rechecked`.

Один `waitLockStudentAlfa(cid)` на журнал+кассу. Вложенный lock не делать: 20 с и второй загрузчик.

`mark()` при этом прогоне не пишет `paysAt` / `paysRecheckAt`. Их только блок кассы после `scanDone`. `lessonsRecheckAt` — только если журнал окна закрыт по id (hole=0, extra=0, rows=uniq). Checksum не смотреть.

Таймаут 90 с не резать. Не досидели 50 missing — касса всё равно. Курсор `payFill` не сбрасывать из журнала.

## Шаг 2. Красная `balance && !recheck`

Касса как сейчас. Вправо — `paysScanned || pays`, не шапка ±1 ₽.

Журнал **только если** `lessonsStampShort` и нет галки. Тогда **одна** пачка missing (`dateFrom` с экрана, take 50), не медленный fill, не «сидеть пока short». `shouldRetryShortPeople` для balance **не** включать — иначе шаг 4 снова встанет на одном id.

Сошёлся журнал или галка — `lesson/index` не звать.

`setsClosed` false при равном счёте — в эту пачку missing, не skip (03f). После пачки всё равно касса.

## Шаг 3. Что не менять

- `peopleJobFinished(balance) = paysScanned || pays`
- `skipLeads` на шаге 4
- `shouldRetryCash` красной
- один `crm-pays.json`
- шаг 5

## Критерий A

1. Тест: `balance+recheck` → census окна, потом pay. Не только pay.
2. Тест: журнал окна не закрыт, pay `scanDone` → `paysRecheckAt` есть, `lessonsRecheckAt` нет, очередь шага 4 не ретраит по short.
3. Тест: `balance` без recheck, журнал готов → pay без census.
4. Тест: `balance` без recheck, short, нет галки → одна missing, потом pay. Не цикл.
5. Тест: `mark` на синей не ставит `paysRecheckAt`.
6. Тест: checksum на `closed` / `paysRecheckAt` не влияет.
7. `admin-crm-settings.tsx` без диффа.

Простыми словами: синяя шага 4 сверяет уроки по номерам за ± месяц, потом читает платежи целиком и идёт дальше, даже если уроки не сошлись. Красная касса; журнал — одна пачка, если дырка, не «Добрать до конца».
