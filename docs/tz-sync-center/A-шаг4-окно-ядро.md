# ТЗ · чат A · шаг 4 ядро

Себе. `pullOneStudent` + тесты. Экран — чат D.

Не трогать: `admin-crm-settings.tsx`, B, C, F, шаг 5, `peopleJobFinished`, `PAY_INBOUND_*`, ромашка. Дефолт `pageCap` census для шага 2 (=12) не менять.

Ключи: `customerId`, `branchId`, `lessonId`, id платежа, `cttId`. Не имя.
Журнал: набор `lessonId` + `diskRows === diskUniq`. Checksum не замок.

## API Alfa

- 5 запросов/с. Limiter в коде нет — не плодить.
- `lesson/index`: без `status` → 3. Даты `YYYY-MM-DD`. Перепись `pageSize` 500.
- Пустые даты census → **2015** и **+90**. Шаг 4: оба конца окна.
- 4 филиала × 3 статуса = 12 запросов на страницы 0.
- Один филиал `ok: false` в текущем census валит всю перепись. На шаге 4: этот филиал пропустить, остальные дочитать. Глобальный census шага 2 не менять (обёртка или `opts`).
- `inboundMissing` до 12 запросов на id. На шаге 4 не звать.
- `pay/index` без дат урока (`yyyy.mm.dd` ≠ уроки).

## Зачем

`if (!balance)` — уроков нет.

Не копировать `else` шага 2: там `inboundMissing` и `windowAlfaKeep`. Если перепись окна нашла новые id и не посадила, а `lessonsAlfa` подняли — **зелёный журнал станет жёлтым**.

`applyCustomerLessonCensus(..., windowFrom, windowTo)` prune только в окне (старые даты не extra). `keepBefore` уже не пишет `lessonsSeenIds` окном. Так и оставить.

## Шаг 1. Синяя `balance && recheck`

Один `waitLockStudentAlfa(cid)`. Не взяли — кассу не начинать. Unlock в `finally`.

1. Касса первая, `{ force: true }`. Без дат урока.
2. Журнал только если `scanDone`.
3. Окно: `clampRecheckDays` → сегодня±N, оба YYYY-MM-DD. Не `recheckCensusWindow` / `wasLessonGreen`.
4. Census с этими датами, `pageCap: 1` (только через аргумент, дефолт шага 2 не трогать).
5. `census.ok` (живые филиалы ответили): `applyCustomerLessonCensus(cid, ids, true, from, to)` — снять ушедшие в окне.
6. Не вызывать: `inboundMissing`, `windowAlfaKeep`, stamp `lessonsAlfa` / `lessonsSeenIds` из окна, `mark()` журнала, `lessonsRecheckAt`.
7. `census.ok === false` — prune нет. Касса уже на диске.

`shouldRetryOpenRecheck(balance)` только `!paysRechecked`.  
`paysAt` / `paysRecheckAt` — только блок кассы после `scanDone`.

## Шаг 2. Красная

Только касса. `lesson/index` нет. Вправо `paysScanned || pays`.

## Критерий A

1. Pay до lesson.
2. Нет `scanDone` → lesson нет.
3. Синяя: оба конца окна, не 2015, не +90.
4. Нет `inboundMissing`. Нет stamp `lessonsAlfa` с длины окна.
5. Тест: диск 100 уроков, окно нашло +2 не посадило — `lessonsAlfa` не вырос, short не появился.
6. Тест: prune снял id в окне, id старше окна на диске остались.
7. Красная без lesson.
8. `pageCap` по умолчанию у census остался 12.
9. `admin-crm-settings.tsx` без диффа.

Простыми словами: касса первая. Синяя после неё снимает с календаря уроки, которых в окне ± месяц уже нет в Alfa. Новые не добирает и счёт Alfa не поднимает — иначе зелёные пожелтеют. Дырки — шаг 2.
