# ТЗ · чат A · шаг 4 ядро

Себе. `pullOneStudent` + тесты. Экран — чат D.

Не трогать: `admin-crm-settings.tsx`, B, C, F, `markPayJournalIncomplete`, шаг 5, `peopleJobFinished`, `PAY_INBOUND_*`, ромашка.

Ключи: `customerId`, `branchId`, id платежа, `cttId`. Не имя. Не `lessonId` на этом шаге.

## Закон

Шаг 4 **грузит кассу**. Рубли и остаток — шаг 5, не открывать.  
Журнал уроков и списания с занятий — шаг 2/3, на шаге 4 `lesson/index` нет.

Готово по загрузке: страницы `pay/index` кончились (`payFill.done`) и на диске нет двух строк с одним id (`uniq === rows`).  
`uniq === rows` — нет дублей, не «в Alfa столько же». Полнота — `total`/`count` страниц.

Вправо как сейчас: `paysScanned || pays`. Дубли не держат очередь (Крюкова). `peopleJobFinished` не менять.

## API Alfa

- 5 запросов/с. Limiter не плодить.
- `POST /v2api/{branch}/pay/index`, фильтры в теле. `customer_id`. Даты кассы `yyyy.mm.dd`, **окно уроков не передавать**.
- Все виды: обычный индекс + после короткой страницы типы 2, 3, 5, 6, 9 — как сейчас, `PAY_INBOUND_*` не трогать.
- `force: true` → `markPayJournalIncomplete` **стирает курсор**. Не звать, пока `payFillPending`.

## Синяя `balance && recheck`

Один `waitLockStudentAlfa(cid)`. Не взяли — не начинать.

1. Касса. `payFillPending` → `force: false` (страница как была). Иначе `force: true`.
2. `paysRecheckAt` только после `scanDone`.
3. **Нет** `censusCustomerLessonIds`, `inboundMissing`, `applyCustomerLessonCensus`, `windowAlfaKeep`, `mark()` журнала, `lessonsRecheckAt`.
4. `recheckDays` / `dateFrom` с экрана в Alfa не слать.

`shouldRetryOpenRecheck(balance)` только `!paysRechecked`.

## Красная `balance && !recheck`

Только касса, курсор не сбрасывать. `dateFrom` в pay не идёт.

## Критерий A

1. Нет ни одного `lesson/index` на `peopleKind: balance`.
2. Pay: `customer_id`, без `date_from` урока.
3. Курсор `{ page: 2 }` + recheck → `page: 2`, не 0.
4. Нет `scanDone` → нет `paysRecheckAt`.
5. Шаг 5, F, экран, `PAY_INBOUND_*` без диффа.
6. Тест: две строки одного pay id — `uniq !== rows`, очередь всё равно идёт дальше.

Простыми словами: шаг 4 качает все платежи по id до конца страниц. Дубли считает, но не стопорит. Уроки и рубли не здесь.
