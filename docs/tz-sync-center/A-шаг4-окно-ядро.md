# ТЗ · чат A · шаг 4 ядро

Себе. `pullOneStudent` + тесты. Экран — чат D.

Не трогать: `admin-crm-settings.tsx`, B, C, F, `markPayJournalIncomplete`, шаг 5, `peopleJobFinished`, `PAY_INBOUND_*`, ромашка. Дефолт `pageCap` census шага 2 (=12) не менять.

Ключи: `customerId`, `branchId`, `lessonId`, id платежа, `cttId`. Не имя.
Журнал: набор `lessonId` + `diskRows === diskUniq`. Checksum не замок.

## API Alfa

- 5 запросов/с. Limiter нет — не плодить.
- Урок в `{branch}` URL. Без `status` → 3. Даты `YYYY-MM-DD`. `pageSize` 500.
- Пустые даты census → 2015 и +90. Шаг 4: оба конца, `ymd(ruShift(±N))`.
- 4×3 = 12 запросов на страницы 0.
- Филиал молчит → prune нет.
- Missing нет. `pay/index` без дат урока.

## Зачем

`if (!balance)` — уроков нет. `else` шага 2 желтит зелёных.

`force: true` каждый тик зовёт `markPayJournalIncomplete` → **стирает `payFill`**. Курсор с страницы 2 снова 0. 4 страницы, не `scanDone`, журнал не стартует, `paysRechecked` нет, синяя крутит того же с нуля. Крюкова.

## Шаг 1. Синяя `balance && recheck`

Один `waitLockStudentAlfa(cid)`. Не взяли — кассу не начинать.

1. Касса первая, **без дат урока**.
   - `payFillPending` (курсор есть, `done` нет) → `inboundCustomerPays` **`force: false`** — продолжить страницу.
   - иначе → `force: true` (новая перечитка).
   - `markPayJournalIncomplete` / F не менять.
2. Журнал только если **этот** вызов дошёл до `scanDone` (`payFill.done`).
3. Окно: `clampRecheckDays` + `ruShift(±N)` → YYYY-MM-DD. Не `wasLessonGreen`.
4. Census, `pageCap: 1` аргументом.
5. Только `census.ok`: `applyCustomerLessonCensus(cid, ids, true, from, to)`.
6. Нет: missing, `windowAlfaKeep`, stamp `lessonsAlfa` / `lessonsSeenIds`, `mark()` журнала, `lessonsRecheckAt`.
7. `!census.ok` — календарь не трогать.

`shouldRetryOpenRecheck(balance)` только `!paysRechecked`.  
`paysRecheckAt` — блок кассы после `scanDone`.

## Шаг 2. Красная

Только касса. Как сейчас, курсор не сбрасывать (уже `force` не на каждый тик очереди). Вправо `paysScanned || pays`.

## Критерий A

1. Pay до lesson.
2. Тест: `payFill` `{ bid: 1, page: 2 }` + recheck → `pay/index` `page: 2`, не 0. `force` нет.
3. Нет `scanDone` → lesson нет.
4. Окно не 2015.
5. Нет missing, нет роста `lessonsAlfa`.
6. Prune в окне, старше окна на диске.
7. Филиал `ok:false` — календарь как был.
8. Дефолт `pageCap` = 12.
9. Экран без диффа. F без диффа.

Простыми словами: касса первая, курсор не сбрасывать на каждом тике. Синяя снимает уроки за ± месяц, только когда страницы кассы дочитаны и все филиалы ответили. Новые не добирает.
