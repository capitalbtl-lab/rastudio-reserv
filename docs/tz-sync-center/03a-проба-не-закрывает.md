# 3a. Проба не закрывает ученика

**REV:** 2026-09-13-probe-keep  
**Ядро A.** F не копирует `keepAlfaProbe` и не заводит второй штамп.  
**Не делать:** `peopleFinished` / колонка, `admin-crm-settings.tsx`, второй `crm-customer-sync`, s20 с экрана, ромашка, деплой.  
**Не класть сюда** xlsx Ежковой: дырка одного `lessonId` в календаре — постановка F, не штамп.

Цель: проба и штамп не закрывают `customerId` и не стирают известный счёт Alfa. Пока диск < Alfa после живой пробы — `short`.

---

## Закон — один, `keepAlfaProbe`

`src/data/crm-inbound-core.ts`. Читают: `pullOneStudent` → `mark()`, ветка probe, `applyCustomerLessonCensus`.

- Молчание / 429 / catch — `lessonsAlfa` и `lessonsAlfaAt` не трогать.
- Живая перепись пишет число как есть. Keep не держит старое (11 при живых 13 — писать 13; 286 при живых 250 — писать 250).
- Окно ±месяц не подменяет полный счёт меньшим куском (`windowAlfaLive`). «С начала · 2015» — длина переписи.
- `lessonsDisk` можно обновить с календаря.
- `lessonsFull` / `lessonsAttend` / `lessonsRecheckAt` — только при равенстве: probedOk && write && !short && !extra.
- Красная skip inbound только если проба живая, диск ≥ Alfa, нет extra.

`lessonsCountShort` / `lessonsCountExtra` / `lessonsJournalReady` не расширять.

---

## Факт 13.09 (диск, не имя)

| cid | Факт | Закон |
|---|---|---|
| 2124 | keep 286 затёрли в 254 (census `uniq.length`) — вернули руками | живая перепись пишет 254; молчание не трогает |
| 5497 | 338/339, full нет | штамп ок; одно занятие в календарь — F |
| 55 | 495 < 506 | не закрывать, не писать Alfa < 506 |
| 4440 | 145 = 145 | закрыт верно, не образец бага |

Воркер `rastudio-history` читает `src/`. После правки штампа — `pm2 restart rastudio-history`.

Простыми словами: проба больше не может сказать «готово», если Alfa молчала или насчитала меньше, чем уже лежит на диске.
