# Шаг 4 как 2 и 3 — указатель

Касса первая. Синяя: `force` только если нет незакрытого `payFill` (иначе курсор в ноль). Журнал после `scanDone`. Prune окна без missing и без `lessonsAlfa`. Молчание филиала — не prune. Красная — только касса.

| Чат | Файл |
|---|---|
| D экран | [D-шаг4-окна.md](D-шаг4-окна.md) |
| A ядро | [A-шаг4-окно-ядро.md](A-шаг4-окно-ядро.md) |
