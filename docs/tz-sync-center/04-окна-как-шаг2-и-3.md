# ТЗ · шаг 4 как шаги 2 и 3

Себе. Экран D + ядро A. Два куска, не мешать файлы.

Не трогать: hero/SEO (B), редактор (C), касса-процесс F (pay.create/delete, poll 10/час), шаг 5.
Деплой — A, после обоих кусков или после D, если ядро уже влито.

## Зачем

Шаги 2 и 3: красная рамка со своим списком, синяя рамка с **± месяц / ± три / ± шесть**, окно в обе стороны от сегодня.
Шаг 4: две кнопки подряд и **один** список лет справа от обеих. Рамок нет. Синего окна нет. `YearsSelect` общий с шагом 2 (`peopleFromId`) — с шага 2 тащится «7 лет».

Ядро: при `kind=balance` календарь **не** ходит (`if (!balance)`). Синяя шага 4 только касса. Подсказка врёт: «сначала пересчёт календаря, потом касса». Список лет на шаге 4 ядро не читает.

Нужно: шаг 4 **того же вида**, что 2 и 3. Синяя шага 4 — то же окно календаря ±N, затем касса. Красная — касса; годы режут календарь, только если его ещё нет. Платежи окном дат не режем.

## Закон

Правда на диске. Alfa — очередь. Ключи: `customerId`, `branchId`, `lessonId`, id платежа, `cttId`, `tariffId`. Не имя.
Окно синей — не годы красной. Не подменять.
Касса не имеет `recheckDays`. `pay/index` как сейчас (страницы, курсор, `paysScanned`). Сумма с шапкой — шаг 5, не «вправо» шага 4.
`peopleJobFinished(balance)` = `paysScanned || pays`. Не ±1 ₽.
Лидов шаг 4 не берёт (`skipLeads`) — не менять.
Второй мозг кассы, второй файл `crm-pays.json` — нет.
`BtnCluster` не писать с нуля.

---

# Кусок D · экран

Файл: `src/components/admin-crm-settings.tsx`. `src/data/*` не трогать.

## Как сейчас (сломано)

```
[Загрузить по одному] [Перепроверить по одному] [YearsSelect 2015/7/3/1] [Стоп]
```

Нет `BtnCluster`. Нет `RecheckDaysSelect`. `windowSel` на карточке кассы нет.
`loadPerson(..., "balance", true)` шлёт `peopleRecheckDays` с шага 2. На шаге 4 человек это не видит.

## Как должно выглядеть

Тот же `RecheckDaysSelect`, подписи уже **± месяц / ± три / ± шесть** (шаг 2/3). Не копировать компонент.

```
BtnCluster red:  [Загрузить по одному] [YearsSelect hint=yearsMoney]
BtnCluster sky:  [Перепроверить по одному] [RecheckDaysSelect]
[Стоп]
```

Не ставить годы в синюю рамку. Не ставить ± в красную. Не добавлять «Медленный автодобор» и «Сверить счёт».

Состояние: `moneyRecheckDays: 32 | 92 | 182`, по умолчанию 32. Не делить с `peopleRecheckDays` и `groupsRecheckDays`.

Карточка шага 4: `years={YearsSelect small}` как сейчас + `windowSel={RecheckDaysSelect small value=moneyRecheckDays}`. Второго выпадающего «с нуля» не плодить.

## Что передать

`recheckPeople("balance", study, true)` и `loadPerson(row, "balance", study, true)`:

```
recheckDays: moneyRecheckDays
```

Красная (`recheck: false`): `dateFrom: peopleDateFrom(peopleFromId)`, **без** `recheckDays`.

`startHistJob` / `runJournal` поле уже есть.

## Подсказки `i`

`HINT.recheckWindow` — уже общий «в обе стороны». На шаге 4 тот же текст, не писать «только шаг 2».

`HINT.recheckOneMoney`:

> Синяя очередь справа на шаге 4. Окно — список в этой же синей рамке (± месяц / ± три / ± шесть), не годы из красной. Сначала тот же пересчёт календаря, что синяя шага 2: набор `lessonId` в окне, новые дописать, ушедшие в окне снять, старше окна не трогать. Потом касса этого `customerId` (страницы, не окно дат). Вправо — страницы кассы дочитаны (`paysScanned`), не совпадение суммы с шапкой. Пауза 5 с. Стоп после текущего. В Alfa не пишет.

`HINT.loadOneMoney` — вычеркнуть «Направо только если остаток совпал с шапкой Alfa ±1 ₽». Писать:

> Красная очередь слева, по одному, пауза 5 с. Касса на диск. Вправо, когда страницы дочитаны (`paysScanned`) или касса уже была. Сумма с шапкой — шаг 5. Календарь подгружает, только если его ещё нет; годы — список в этой же красной рамке, режут занятия, не платежи. Синяя рамка не читается. В Alfa оплаты не создаёт.

`HINT.yearsMoney`:

> Только красные кнопки шага 4 и «Загрузить кассу» на карточке. Те же годы, что шаг 2. По умолчанию «с начала · 2015». Режет календарь, если его ещё нет. Платежи не режет. Синяя рамка это не читает.

`HINT.recheckPay`: «окно лет» → «окно синей рамки (± месяц / ± три / ± шесть) для журнала; касса — все страницы».

`HINT.tabMoney`: как шаг 2 — красная рамка годы, синяя рамка ±. Не «справа те же годы» на обе кнопки.

Гид шага 4: одна строка — синяя ±N по журналу, потом касса без окна дат. Список клиентов не писать.

## Не делать (D)

Ядро, `crm-pay.ts`, очередь, шаг 5, лиды в кассу, деплой.

---

# Кусок A · ядро

Файлы: `src/data/crm-journal-pull.ts` (`pullOneStudent`), тесты. Экран не трогать.

## Дыра

```
if (!balance) {
  красная: проба / missing
  синяя: окно ±N
}
if (balance) {
  только inboundCustomerPays
}
```

Синяя шага 4 не видит `recheckDays`. Красная не видит `dateFrom` для журнала.

## Что сделать

В `pullOneStudent` при `balance === true`:

**Синяя (`recheck === true`).** Сначала тот же блок, что сейчас в `else` (окно `recheckCensusWindow`, prune, missing новых, `keepAlfaProbe` / `windowAlfaKeep`, галка). Не 2015. Не полный fill. `take` missing 50. Потом касса как сейчас: `inboundCustomerPays`, курсор, `paysRecheckAt` после `scanDone`. Не ставить `paysRecheckAt` из `mark()` календаря.

**Красная (`recheck === false`).** Касса как сейчас. Журнал — **только если** слева по журналу ещё дырка: `lessonsStampShort` и нет галки. Тогда тот же missing, что красная шага 2 (`dateFrom` с экрана, без страниц качки). Если журнал уже сошёлся или галка — календарь не трогать. `setsClosed` / hole: равный счёт при разных id не skip (закон 03f).

Касса: не фильтровать `pay/index` окном ±N. `paysComplete` / ±1 ₽ не возвращать в «вправо».

## Не делать (A)

`pageSize` кассы, poll, `pay.create`, второй файл кассы, `peopleJobFinished`, экран D, ромашка.

## Критерий готово

1. Шаг 4: красная рамка + годы, синяя рамка + ± месяц/три/шесть, Стоп снаружи. Как скрины 2 и 3.
2. «± три» на шаге 4 не равно значению шага 2. Job синей: `recheckDays: 92`, `peopleKind: balance`.
3. Синяя шага 4 в логе: сначала census/missing окна, потом pay. Не только pay.
4. Красная шага 4 с журналом на месте: только касса. Годы не ходят в `lesson/index`.
5. Красная шага 4 с дыркой журнала: missing по `dateFrom` лет, потом касса.
6. Человек с `paysScanned` справа, даже если шапка 309 уроков не сходится. Шаг 5 не открывать.
7. Тесты: balance+recheck вызывает окно; balance без recheck при готовом журнале — pay без census.
8. `src/components/admin-crm-settings.tsx` в куске A не диффается.

Простыми словами: шаг 4 выглядит как 2 и 3. Красное грузит кассу (и календарь, только если его нет). Синее за месяц вперёд и назад проверяет уроки, потом заново читает платежи. Сумму с шапкой не требует — это шаг 5.
