ТЗ · ядро A · 3b синяя + галка «одобрить»

Цель. Extra не качает журнал с 2015. Одобренная дырка не качается никем, кроме явной «Добрать» на карточке. Prune не затирает чужой загрузчик. Проба 3a жива.

Закон. Правда на диске. Alfa — очередь. Ключи: customerId, lessonId, branchId, groupId. Не имя. Один штамп crm-customer-sync.json, одна очередь пакетов, один people job. Экран D не трогать: галка уже шлёт holeApprove. Ромашка / деплой — только если скажут.

Факт с экрана 13.09
- Слева short: 2124 244/286, 3283, 5115, 699. Галка = не Добрать, журнал не закрыть.
- Справа extra: 5036 172/171. Синяя «дубли, снять».
- Ядро peopleJobFinished / shouldRetryShortPeople / inboundCustomerLessonsChunk галку не читают — Фон и ретрай снова качнут одобренного.
- census: page < 12, полная 12-я → ok:true. Prune по этому сотрёт хвост.
- Синяя сейчас: inbound full × 6 с 2015, потом census, потом prune без защиты группового диска.

Место правки
- src/data/crm-customer-sync.ts — ключ уже есть, не расширять lessonsJournalReady
- src/data/crm-journal-job-core.ts — PeopleJobRow.holeApproved; peopleJobFinished / peopleJobQueue / shouldRetryShortPeople
- src/data/crm-journal-inbound.ts — census close, applyCustomerLessonCensus prune, inboundCustomerLessonsChunk skip
- src/data/crm-journal-pull.ts — ветка recheck=true в pullOneStudent; mark/проба не трогают journalHoleApprovedAt (уже)
- src/data/crm-inbound-core.ts — canCloseLessonCensus / prune: не закрывать полную страницу; prune держит id с группы
- тесты рядом: crm-journal-job.test.ts, crm-journal-inbound / pull / inbound-core
Не трогать: admin-crm-settings.tsx, PeopleRow, hero/SEO, второй файл штампа, второй воркер

Правка 1. Очередь и Фон чтят галку
- holeApproved = Boolean(journalHoleApprovedAt)
- peopleJobFinished: short && holeApproved → как «не в красной» (finished для needLoad), short без галки → false
- shouldRetryShortPeople: holeApproved → false
- inboundCustomerLessonsChunk: если stamp.journalHoleApprovedAt и lessonsCountShort — continue, не inboundCustomerLessons
- journalPeopleSide уже отдаёт holeApproved — в job items с экрана поле должно доехать до PeopleJobRow

Правка 2. Перепись не закрывается обрезанной
- censusCustomerLessonIds: если live.ok и items.length===pageSize на последней крученой странице (упрёмся в потолок или не break) → return ok:false, aborted
- убрать ложное ok после ровно 12 страниц по 100
- 429/catch страницы → ok:false как сейчас
- prune и запись lessonsAlfa с uniq.length — только census.ok

Правка 3. Синяя без полного журнала
pullOneStudent recheck=true:
- не вызывать inboundCustomerLessons циклом с 2015
- census; если !ok → mark диск, probedOk false (3a), выйти
- disk vs uniq.length
- extra → applyCustomerLessonCensus с защитой шага 4
- short && !holeApproved → inboundMissing по census.ids − disk, cap 50
- short && holeApproved → missing не звать, lessonsFull не ставить, галку не снимать
- равенство → mark closed / lessonsRecheckAt
«Сверить счёт» (probe без recheck) не качает и не prune — как было

Правка 4. Prune не жрёт чужое
applyCustomerLessonCensus / pruneCalendarToAlfaIds:
- держать: lessonId<0, hold lesson.update/create, id которые есть в calendar группы ученика (groupLinks → loadGroupCard → lessonId)
- остальное, чего нет в закрытой переписи — снять
- закрытая перепись: lessonsAlfa = uniq.length
- holeApproved: lessonsFull не ставить, даже если после prune диск==alfa (дырку человек не закрывал; extra снять можно)

Не делать
- окно 1/2/4 мес на перепись или prune
- syncAllFromCrm, clientsArchive, leads
- снимать journalHoleApprovedAt из mark / inbound / Фон
- менять галочки публикации, тексты главной
- второй мозг очереди
- править D, даже если синяя очередь экрана ещё кладёт одобренных в jobItems: ядро skip по штампу

Приёмка на диске, не по имени
1. holeApprove 2124: ключ ISO, lessonsFull false, 244 и 286 на месте. Chunk Фон и people job без этого cid в inbound.
2. holeApproveClear: ключа нет.
3. Census: страница 100 на потолке → ok false, файл календаря байт-в-байт, lessonsAlfa тот же.
4. 5036: id, который есть на group-cards её группы и нет в customer_id index — после синей на диске cid.
5. id только в личном календаре, перепись закрыта без него, галки нет — снят, lessonsAlfa=uniq.
6. Синяя по 2124 с галкой: нет missing, нет lessonsFull, ключ на месте.
7. «Добрать» без галки: keep 3a как 03a.
8. Тест: recheck ветка не содержит inboundCustomerLessons; chunk содержит journalHoleApprovedAt skip.

Простыми словами. Галка — не грузить. Синяя — пересчитать номера, не гонять 2015, не стереть журнал группы. Обрезанная перепись — как молчание: ничего не резать.
