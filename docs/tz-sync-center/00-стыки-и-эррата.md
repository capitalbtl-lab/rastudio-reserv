# Пакет этапов 1–8. Стыки (REV 2026-09-10-pack-fix)

Родитель: ТЗ sync-center-3 + вклейка 3a. Рамашка 12 не трогать.

## Что починено в текстах

1. **Этап 3 восстановлен** (штампы). Файл «ЭТАП 3.docx» был копией этапа 2 — не использовать.
2. **Порядок дирижёра один на 2/4/5**, дырки skip, не перестановка в этапе 5.
3. **Poll кассы:** только cron + лок. Conductor `pollPaysFromAlfa` не зовёт.
4. **Старт окна** идёт через `canSavePolicy`.
5. **Лето:** один хелпер `canOfferSummer` с этапа 5.
6. **Касса cap:** 4000 строк на ученика.
7. **Этап 8** после этапа 3; фраза «грузить нечего» убрать.
8. **`history.hour`:** минуты от полуночи (270 = 04:30).
9. **`nightHour`:** на диске с этапа 1, в рантайм каталога **не** включать в 2/4 (трекер).
10. **`yearPresets`:** диск; селект шага 1 пока свой (`PEOPLE_FROM_OPTS`).
11. **Проба не закрывает cid** (2026-09-13): `keepAlfaProbe` — сорванная/слабая проба не стирает `lessonsAlfa` и не skip inbound. Файл `03a-проба-не-закрывает.md`. Сорванная перепись не пишет `uniq.length` ниже keep.
12. **Синяя не затирает чужую качку и галку** (2026-09-13): окно дат не для prune; перепись с полной последней страницей не закрыта; id с группового диска не снимать; `journalHoleApprovedAt` — skip качки, не `lessonsFull`. Файл `03b-синяя-окно-не-затирает.md`.
13. **Лёд / cap / волны / keep** (2026-09-15): синяя пишет `dateFrom`/`dateTo` один раз; красная `dateTo=""`; cap 8 не хоронит resume (`waits:0`, потом левые); `n>=total` не отказывает волнам; prune и минус keep только закрытая перепись. Файл `03h-лёд-cap-волны-keep.md`.
14. **Пульт автомата Истории** (2026-09-15): кнопка слева от «Шаг 1» во вкладке «История из Alfa», открывает всплывающее окно. Не отдельная вкладка и не селект off/everyNh. Конструктор «когда + режим кнопки», мастер вкл, один джоб. Файл `01b-конструктор-расписания.md`.

## Порядок внедрения

`1 политика → 2 дирижёр → 3 штампы → 4 окно → 5 коллеги → 6 +2 → 7 дайджест → 8 UI`

4 и 8 **запрещены** без этапа 3.

## Дирижёр (сквозной)

```
1 export          (очередь исходящая, take 1)
2 payPoll         SKIP: poll живёт в cron rastudio-pay-poll + tryLock("payPoll")
3 colleague       этап 5; иначе skip
4 delta           клиенты / лиды по minutes
5 window          этап 4; иначе skip
6 drip            только policy.dripQuarters && канал Журнал
```

Пустой export не блокирует ниже. Manual / night: inbound skip после текущего пакета. Один пакет за тик.

## Штампы (сквозные)

| Поток | Пишет | Не пишет |
|---|---|---|
| Шаг 1 руками | journal, rechecked календаря | pays*, alfa probe, windowAt, pulled квартала группы |
| «Сверить число» | alfa (счётчик занятий) | rechecked, pays |
| Шаг 2 квартал | pulled/done/rechecked/weak зерна | windowAt как ✓, pays |
| Шаг 3 | pays, paysRechecked | rechecked календаря |
| Окно 14 дн. | windowAt / paysWindowAt | rechecked, lessonsFull, pulled[q], колонка «завершена» |
| Коллеги 3 дн. | occupancy, colleagueAt | lessonsFull, pulled, правый столбец |
| Poll кассы | poll fill | complete[] всей кассы, paysRechecked |

## Не делать ни в одном этапе

enqueueExport из истории/окна/коллег/дайджеста. pickTeacherIds. ProgressiveBar. Ломать 658 inspect. «Догрузить текущих» как проверка выкладки.
