# Этап 1. Политика центра связи на диске

**Для кого:** следующий прогон «делай» — только это.  
**REV:** 2026-09-10-stage-1-policy-fix  
**Родитель:** ТЗ sync-center-3 + вклейка 3a  
**Не делать:** дирижёр, серверное окно, отработки +2 в коде записи, дайджест ИИ, аккордеон, качка Alfa, деплой «заодно».

Цель: администратор **без деплоя** крутит все числа ТЗ, они лежат на диске, со следующего тика **читаются**. Новое поведение ещё **выкл**. Живой сайт и История как сейчас.

---

## 0. Перед правками

1. Бэкап **Ромашка 13**, если ещё нет: тег git + tar исходников без `node_modules` / `.output`.
2. Не трогать Рамашку 12: `crm-group-teachers-core`, `crm-group-export-core`, `pushSlotsToCrm` inspect, `pickTeacherIds` нет.
3. Не трогать `journalPull`, `inboundJournal*`, merge квартала, снимок `group-cards.json`.
4. Не менять интервал `startAlfaIdleTick` (20 с) в этом этапе — только сохранить поле `tickSec`. Тик начнёт его слушать на этапе 2.

---

## 1. Что уже есть — опереться, не дублировать

| Сейчас | Файл | Этап 1 |
|---|---|---|
| linked / offline, pull / push / pipe, minutes, payDays | `storage/crm-alfa-link.json`, `crm-alfa-link.ts`, `crm-alfa-link-core.ts` | оставить. Галочки как есть |
| Тик 20 с хардкод | `crm-packet-queue.ts` | не менять интервал |
| Касса за N дней | UI Фон, `alfaPayDays()` | уже 1–14, оставить |
| `consultantCanBook*` | Окно ассистента | **не копировать** в новый блок |
| История шаги 1–3 | `admin-crm-settings.tsx` `crmTab === "history"` | **не ломать вёрстку**, только карточка настроек **над** шагами |

`minutes` сейчас = «сверять CRM каждые N мин» (дельта людей), диапазон 2–60, UI 5/10/15/30. Это **не** тик очереди и **не** cron poll. Подписать явно: «Дельта клиентов и лидов, мин». Не смешивать с `tickSec`.

---

## 2. Новые файлы

### 2.1. Ядро без fs

`src/data/crm-sync-policy-core.ts`

Тип `CrmSyncPolicy` + `POLICY_FACTORY` + `policyOf(raw)`:

- неизвестные ключи игнорировать;
- вне диапазона → **завод этого поля**, не NaN;
- частичный JSON → дырки из завода;
- `policyOf` **чистый**, без диска.

`canSavePolicy(p)`:

- если `history.enabled && background.dripQuarters` → `{ ok: false, error: "оставьте одно: капельница кварталов или окно истории" }`;
- `inspectRegular` всегда `true`, с диска не читается как false;
- `packetsPerTick` всегда 1.

«Как было» = `POLICY_FACTORY`, не «последнее удачное».

### 2.2. Диск

`src/data/crm-sync-policy.ts`

- путь: `storage/crm-sync-policy.json`
- `loadSyncPolicy()` / `saveSyncPolicy(patch)`
- запись через уже принятый атомарный write
- save зовёт `policyOf` + `canSavePolicy`; отказ — старый файл **не** трогать, вернуть `{ ok: false, error, policy: current }`
- **не** вызывать `startAlfaIdleTick`, `journalPull`, очередь

ИИ и фон на этапах 2+ читают `loadSyncPolicy()`, не константы.

---

## 3. Завод и диапазоны

Все ключи завести сразу, даже если UI поля disabled («ещё не работает»). Лучше показать серым, чем не завести.

### Фон (каналы остаются в alfa-link)

| Ключ | Завод | Диапазон | Смысл | Когда читает рантайм |
|---|---|---|---|---|
| `tickSec` | 20 | 10–60 | тик очереди | этап 2. UI: «с этапа 2; сейчас в коде 20 с» |
| `pause429Sec` | 60 | 30–180 | пауза 429 | этап 2 |
| `colleagueDays` | 3 | 0 = выкл, иначе 1–14 | занятия коллег | этап 5 |
| `payCronMin` | 15 | 0 = выкл, иначе 5 / 15 / 30 / 60 | интервал процесса poll | этап 5: cron, не дирижёр. UI: «процесс 15 мин, смена с деплоем» |
| `nightHour` | 4 | −1 = выкл, иначе 0–6 | час каталога МСК | **не читать в 1–4**. Каталог 04:00 как был. UI: «на диске, каталог пока как был» |
| `dripQuarters` | **false** | bool | капельница кварталов | этап 2 |
| `inspectRegular` | true | нельзя false | сверка шаблона | Рамашка 12. UI серый текст, не чекбокс |

`payDays` и `minutes` **не дублировать** — читать из `loadAlfaLink()`.

### История (окно)

| Ключ | Завод |
|---|---|
| `history.enabled` | false |
| `history.schedule` | `"off"` (`off` / `afterCatalog` / `everyNh`) |
| `history.everyHours` | 6, диапазон 1–24 |
| `history.hour` | **270** — минуты от полуночи (в UI «04:30»). Не хранить 4.5. Час слота 0:00–7:00 → 0–420 |
| `history.days` | 14; пресеты 7 / 14 / 30 / quarter / своё 1–90. `policyOf`: 999 → **clamp 90** |
| `history.archiveStudents` | false |
| `history.archiveGroups` | false |
| `history.pauseSec` | 15, 5–60 |
| `history.packetsPerTick` | 1, UI не даёт иное |
| `history.steps` | `{ calendar: true, groups: true, money: true }` |
| `history.shortNotComplete` | true |
| `history.zeroNotDone` | true |
| `history.cappedYellow` | true |
| `history.windowPullsDetails` | **false** (Д2) |
| `history.yearPresets` | `[{label:"с 2015",from:"2015-01-01"},{label:"7 лет",years:7},{label:"3 года",years:3},{label:"год",years:1}]`. На диске. Селект шага 1 (`PEOPLE_FROM_OPTS`) **пока свой**. Не два источника в одном жесте. Подпись: «пресеты на диске, шаг 1 подключит позже» |
| `history.grain` | `"quarter"` (`quarter` / `half` / `year`) |
| `history.manualPauseSec` | 5, 2–30 **отдельно** от 15 |
| `history.archiveBatch` | 10, 1–30 |
| `history.askAllArchives` | true |
| `history.detailsBatch` | 16, 4–32 |
| `history.studentsBatch` | 10, 1–50 |

Окно **не запускать**. Поля только хранить.

### Запись / дайджест

| Ключ | Завод | Диапазон |
|---|---|---|
| `makeup.extra` | 2 | 0–10 |
| `makeup.perLesson` | true | выкл только с предупреждением в UI |
| `makeup.offerSlots` | 3 | 1–8 |
| `makeup.writeCgi` | false | |
| `makeup.ownGid` | false | |
| `makeup.fullGroupOk` | true | |
| `digest.futureDays` | 21 | 7–45 |
| `digest.pastLessons` | 4 | 1–8 |
| `digest.topic` `hw` `grade` `note` | true | |
| `digest.peerGrades` | **всегда false**, ключа выкл нет | |
| `pull.adminVoice` | true | |
| `pull.siteConsultant` | false | |
| `speech.moneyAndLessonsSplit` | true | |
| `live.summer` | false | |
| `live.regular` | false | сверка ритма не выключается |

Смена `makeup.extra` **не** пересчитывает уже стоящие отработки.

---

## 4. UI

Файл: `admin-crm-settings.tsx`. Кнопки шагов 1–3 **не двигать**. Не вставлять ProgressiveBar. Не писать «грузить нечего» в этой карточке.

### 4.1. Фон (`crmTab === "alfa"`)

Существующую карточку не ломать. Блок **«Темп»** под «Труба»:

- Тик очереди, сек (`tickSec`) — «с этапа 2; сейчас в коде 20 с».
- Пауза после 429 (`pause429Sec`).
- Занятия коллег, дни (`colleagueDays`, 0 = выкл).
- Cron poll кассы (`payCronMin`) рядом с «Касса за N дн» — «процесс 15 мин, смена с деплоем».
- Ночь каталога, час МСК (`nightHour`, «выкл» = −1) — «на диске, каталог пока как был».
- Капельница кварталов (`dripQuarters`) — **выкл**, подсказка: «журнал вглубь — вкладка История. Вместе с окном истории сохранить нельзя».

Подсказка сверху: «Диск — правда. Числа ниже — на сервере. Журнал явок вглубь — вкладка История».

`minutes` переименовать в подписи: «Дельта клиентов и лидов, мин». Второй селект в «Люди в Alfa» оставить тем же значением (не два источника).

Галочки pull/push/pipe, linked/offline — как сейчас. Сохранение каналов — `saveAlfaLink`. Новые числа — `saveSyncPolicy`. Если админ в одном жесте меняет канал и политику — два запроса, каналы не ждут политику.

### 4.2. История — карточка над шагами

Заголовок: «Фоновая перепроверка (сервер)».  
Подзаголовок: «Пока выкл. Шаги 1–3 ниже как раньше. Вкл — на этапе 4, без зелёного “всё есть” за 14 дней».

Поля `history.*` из таблицы.  
Кнопки Старт/Стоп **не** рисовать (иначе нажмут в пустоту). Серый текст: «Старт появится, когда прогон на сервере будет готов».

Кнопка **«Как было»**: сброс **всей** `CrmSyncPolicy` к заводу (не alfa-link mode). Confirm: «Вернуть заводские числа центра связи?»

### 4.3. «Запись и отработки»

Либо подвал Истории, либо якорь из Окна. Не дублировать `consultantCanBook*`. Строка: «Типы записи — Ассистент ИИ → Окно».

Поля `makeup.*` и `digest.*`.  
Текст ритма: «выгрузка шаблона группы только после сверки (Рамашка 12). Выключить нельзя».  
Лето: галочка + текст «плюс сезон апрель–август, оба условия».  
Чужие оценки: не чекбокс, серое «выкл всегда».  
Отработки: «Запас сверх лимита. Сценарий недели своего педагога не отменяется. Включится в записи на этапе 6».

### 4.4. Ошибки UI

`canSavePolicy` false → красная строка, чекбоксы не «залипают» в запретном состоянии (капельница и окно: кто включил вторым — откат второго).

Вне диапазона в select не показывать; number input blur → clamp через `policyOf`.

---

## 5. API кабинета

В существующий admin action (тот же, что `saveAlfaLink`, не новый публичный URL):

- `action: "syncPolicyGet"` → `{ policy, factory, alfa: { minutes, payDays, mode } }`
- `action: "syncPolicySave"` → patch, ответ `{ ok, policy, error? }`

Токен админки как у остальных CRM actions. **Не** Alfa. **Не** `enqueueExport`.

Загрузка вкладки Настройка CRM: один get политики вместе с alfa-link.

---

## 6. Что этап 1 не меняет в рантайме

| Место | Остаётся |
|---|---|
| `startAlfaIdleTick` 20_000 | хардкод |
| `wantAlfaPullChannel("lessons")` | как сейчас; `dripQuarters` ещё никто не читает |
| `journalPull` / кнопки Истории | как сейчас, force как сейчас |
| `book_lesson` makeup | без формулы +2 |
| промпты ИИ | без дайджеста из политики |
| `crm-pay` `slice(-40000)` | этап 5 |
| легенда статусов, аккордеон | этап 8 |
| два штампа явки/касса | этап 3 |

Единственное рантайм-исключение: `saveSyncPolicy` при конфликте капельница+окно **не пишет**. Оба выкл — ок.

---

## 7. Тесты

Файл `crm-sync-policy-core.test.ts`:

1. Пустой raw → завод, `history.enabled === false`, `dripQuarters === false`.
2. `history.days`: 999 → **90**.
3. `tickSec`: 5 → 10. `pause429Sec`: 10 → 30.
4. `packetsPerTick`: 8 → 1. `inspectRegular`: false → true.
5. `digest.peerGrades`: true → false.
6. `history.hour`: 4.5 не хранить; 270 проходит; 999 → clamp к 420 (7:00) или завод 270 — зафиксировать в тесте **clamp 0–420**.
7. `canSavePolicy` оба вкл → not ok; по отдельности ok.
8. «Как было» глубоко равно `POLICY_FACTORY`.
9. Частичный `{ makeup: { extra: 3 } }` не обнуляет `digest.futureDays`.
10. Ядро **не** импортирует fs / `crm-packet-queue`.

Если есть лёгкий тест save на tmp dir — отказ `canSave` не портит файл.

Не гонять живую Alfa. Не открывать group-cards.

---

## 8. Готово, когда

1. Настройка CRM → Фон есть блок «Темп»; История — серая карточка над шагами; шаги 1–3 открываются и грузят диск как вчера.
2. Сменили 14→7, F5 — 7. Деплоя нет.
3. «Как было» возвращает 14 / +2 / 21 день / окно выкл / капельница выкл / hour 270.
4. Вкл капельницу и окно → отказ, файл на диске старый.
5. `storage/crm-sync-policy.json` есть, секретов нет.
6. `crm-alfa-link.json` не потерял галочки.
7. Тесты §7 зелёные.
8. На сервере **не** появилось новых запросов `lesson/index` от этой задачи.
9. 658 / выгрузка слота / открыл карточку — без изменений очереди.

## 9. Коммит

Один коммит: `Политика центра связи на диске: поля Фон/История/запись, окно выкл, капельница выкл.`

Не включать этап 2 в тот же коммит. После выкладки — только Ctrl+F5 на Настройка CRM, не «Догрузить текущих».

Этап 2 читает `tickSec`, `pause429Sec`, `dripQuarters`.  
Этап 4 читает `history.*`, не стартует если `enabled===false`.  
Этап 6 читает `makeup.*`.  
Этап 7 — `digest.*`, `pull.*`.
