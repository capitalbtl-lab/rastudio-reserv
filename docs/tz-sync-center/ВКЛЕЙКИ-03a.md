# Куда вставить 3a

Канон: `03a-проба-не-закрывает.md`. Закон один: `keepAlfaProbe`.  
Не копировать функцию. Не класть в 3a xlsx Ежковой (дырка одного `lessonId` — постановка F).

---

## `03-штампы.md` — блок «probe ставит только alfa»

После строки про probe / жёлтый short:

```
- **3a.** `keepAlfaProbe`. Молчание Alfa не трогает `lessonsAlfa` / `lessonsAlfaAt`.
  Число меньше keep не писать. `lessonsFull` только при равенстве (!short && !extra).
  Красная skip inbound только если проба живая и не слабее keep.
  Census синей не пишет `uniq.length` в обход. Файл `03a-проба-не-закрывает.md`.
```

---

## `00-стыки.md` — таблица «Штампы (сквозные)»

Строка:

| Поток | Пишет | Не пишет |
|---|---|---|
| Проба / `keepAlfaProbe` | `lessonsAlfa` только если probedOk и не слабее keep | счёт при молчании; заниженный alfa; `lessonsFull` при дырке |

В запреты: не `disk >= alfa0` без keep; не стирать `lessonsAlfaAt` в `""`.

---

## `00-стыки-и-эррата.md` — список «Что починено»

```
11. **Проба не закрывает cid** (2026-09-13): `keepAlfaProbe`.
    Сорванная/слабая проба не стирает `lessonsAlfa` и не skip inbound.
    Census не пишет `uniq.length` ниже keep. Файл `03a-проба-не-закрывает.md`.
```
