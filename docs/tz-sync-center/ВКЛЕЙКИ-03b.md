Канон: `03b-синяя-окно-не-затирает.md`. REV recheck-hole.

`03-штампы.md`:

- **3a.** Проба / «Добрать»: `keepAlfaProbe`. Файл `03a-проба-не-закрывает.md`.
- **3b.** Синяя без журнала с 2015. Prune не трогает id группы / hold / local. `journalHoleApprovedAt`: Фон и красная skip; синяя без missing и без `lessonsFull`; проба/mark/синяя галку не снимают. Файл `03b-синяя-окно-не-затирает.md`.

`00-стыки.md`:

Канон 3b: окно дат не для prune. Потолок 12 страниц не закрывает перепись. Fan-out группы старше `customer_id`. Одобренная дырка — не качка.
