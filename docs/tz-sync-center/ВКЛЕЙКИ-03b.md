Канон: `03b-синяя-окно-не-затирает.md`.

`03-штампы.md` пункт 3a, вместо «Census синей не пишет uniq.length в обход»:

- **3a.** Проба / «Добрать»: `keepAlfaProbe`. Файл `03a-проба-не-закрывает.md`.
- **3b.** Синяя: закрытая полная перепись пишет живое число; prune не трогает id с группового диска / hold / local. Не качать журнал с 2015. Файл `03b-синяя-окно-не-затирает.md`.

`00-стыки.md` после канона 3a:

Канон 3b: `03b-синяя-окно-не-затирает.md`. Окно дат не для prune. Потолок 12 страниц не закрывает перепись. Fan-out группы старше индекса `customer_id`.
