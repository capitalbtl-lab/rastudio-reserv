# ТЗ · чат D · шаг 4 экран

Себе, экран. Ядро — чат A, не трогать `src/data/*`.

Файл: `src/components/admin-crm-settings.tsx`.
Не трогать: hero (B), редактор (C), шаг 5, деплой (A).
`BtnCluster` и `RecheckDaysSelect` уже есть — не копировать.

Ключи на экране не угадывать. Окно синей — не годы красной.

## Зачем

Шаги 2 и 3: красная рамка со своим списком, синяя с **± месяц / ± три / ± шесть**.
Шаг 4: две кнопки подряд и один список лет на обе. Рамок нет. Синего окна нет. Годы общие с шагом 2 (`peopleFromId`) — тащится «7 лет».

Нужно: шаг 4 **того же вида**. Подсказки без «вправо по ±1 ₽».

## Шаг 1. Ряд кнопок

Сейчас:

```
[Загрузить по одному] [Перепроверить по одному] [YearsSelect] [Стоп]
```

Надо:

```
BtnCluster red:  [Загрузить по одному] [YearsSelect hint=yearsMoney]
BtnCluster sky:  [Перепроверить по одному] [RecheckDaysSelect]
[Стоп]
```

Не ставить годы в синюю рамку. Не ставить ± в красную. Не добавлять «Медленный автодобор» и «Сверить счёт».

## Шаг 2. Своё окно, не с шага 2

Состояние: `moneyRecheckDays: 32 | 92 | 182`, по умолчанию 32.
Не делить с `peopleRecheckDays` и `groupsRecheckDays`.

Подписи опций уже **± месяц / ± три / ± шесть** — тот же `RecheckDaysSelect`.

## Шаг 3. Что слать в ядро

Синяя (`recheckPeople("balance", study, true)` и `loadPerson(..., "balance", true)`):

```
recheckDays: moneyRecheckDays
```

Красная (`recheck: false`):

```
dateFrom: peopleDateFrom(peopleFromId)
```

без `recheckDays`.

Поле `recheckDays` у `startHistJob` / `runJournal` уже есть. Даты `сегодня±N` на экране не считать.

## Шаг 4. Карточка

`years={YearsSelect small}` как сейчас.
`windowSel={RecheckDaysSelect small value=moneyRecheckDays}`.
Второго выпадающего «с нуля» не плодить. Кнопка «Перепроверить» на зелёной берёт верхний ±.

## Шаг 5. Подсказки `i`

`HINT.recheckWindow` — общий текст «в обе стороны от сегодня». Не писать «только шаг 2».

`HINT.recheckOneMoney`:

> Синяя очередь справа на шаге 4. Окно — список в этой же синей рамке (± месяц / ± три / ± шесть), не годы из красной. Сначала сверка журнала по номерам уроков в окне (новые дописать, ушедшие в окне снять). Потом касса этого человека — все страницы, не окно дат. Вправо, когда страницы кассы дочитаны, не когда сумма совпала с шапкой. Пауза 5 с. Стоп после текущего. В Alfa не пишет.

`HINT.loadOneMoney` — убрать «Направо только если остаток совпал с шапкой Alfa ±1 ₽»:

> Красная очередь слева, по одному, пауза 5 с. Касса на диск. Вправо, когда страницы дочитаны или касса уже была. Сумма с шапкой — шаг 5. Календарь подгружает, только если его ещё нет; годы — список в этой же красной рамке, режут занятия, не платежи. Синяя рамка не читается. В Alfa оплаты не создаёт.

`HINT.yearsMoney`:

> Только красные кнопки шага 4 и «Загрузить кассу» на карточке. Те же годы, что шаг 2. По умолчанию «с начала · 2015». Режет календарь, если его ещё нет. Платежи не режет. Синяя рамка это не читает.

`HINT.recheckPay`: «окно лет» → «окно синей рамки (± месяц / ± три / ± шесть) для журнала; касса — все страницы».

`HINT.tabMoney`: красная рамка — годы, синяя — ±. Не «справа те же годы» на обе кнопки.

Гид шага 4: одна строка — синяя ±N по журналу, потом касса без окна дат. Список клиентов не писать.

## Не делать

Ядро, `crm-pay.ts`, очередь, шаг 5, лиды в кассу, деплой.

## Критерий D

1. Шаг 4 выглядит как 2 и 3: красная+годы, синяя+±, Стоп снаружи.
2. «± три» на шаге 4 не меняет шаг 2. В job: `recheckDays: 92`, `peopleKind: balance`.
3. Красная шлёт годы, не `recheckDays`.
4. `i` не говорит «вправо по ±1 ₽».
5. `src/data/*` без диффа.

Простыми словами: шаг 4 как 2 и 3 — красное с годами, синее с ± месяцем. Сумму с шапкой экран больше не обещает.
