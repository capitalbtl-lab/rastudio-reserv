# ТЗ · чат D · шаг 4 экран

Себе, экран. Ядро — чат A. `src/data/*` не трогать.

Файл: `src/components/admin-crm-settings.tsx`.
Не трогать: B, C, шаг 5, деплой (A).
`BtnCluster` и `RecheckDaysSelect` уже есть — не копировать.

Годы красной ≠ окно синей. Checksum не показывать. Даты Alfa не считать.

## API (только `i`)

Экран Alfa не зовёт. Синяя шлёт `recheckDays`. Ядро: касса; затем одно окно уроков ±N, без добора дырок.

Не писать: «касса за ± месяц»; «сначала журнал»; «очередь ждёт уроки»; «90 секунд»; «Добрать»; «Касса и журнал на месте» если журнала нет.

Сейчас на зелёной кассы текст **«Касса и журнал на месте»** — ложь, если уроки жёлтые. Заменить на **«Касса на месте»**. Слева: не «Касса и журнал — пока чего-то нет».

## Зачем

Шаги 2–3: красная рамка + список, синяя + ±.  
Шаг 4: две кнопки, один список лет. `peopleFromId` / `peopleRecheckDays` с шага 2.  
Карточка: `kind === "students" && (full || dups) ? windowSel : years`.

## Шаг 1. Ряд

```
BtnCluster red:  [Загрузить по одному] [YearsSelect hint=yearsMoney]
BtnCluster sky:  [Перепроверить по одному] [RecheckDaysSelect]
[Стоп]
```

Не годы в синюю. Не ± в красную. Не «Медленный автодобор» / «Сверить счёт».

## Шаг 2. Свои списки

```
moneyRecheckDays: 32 | 92 | 182   // 32
moneyFromId: PEOPLE_FROM_OPTS     // "2015"
```

Не делить с шагом 2. Красные годы на шаге 4 в Alfa **не идут** (ядро A кассу не режет годами). Список всё равно свой, чтобы «7 лет» не тащилось в job соседней вкладки.

## Шаг 3. Слот

Синяя: `recheckDays: moneyRecheckDays`.  
Красная: `dateFrom: peopleDateFrom(moneyFromId)` можно слать — ядро проигнорирует. Без `recheckDays`.

## Шаг 4. Карточка

`kind === "balance"`: нет кассы → `years`; `full || dups` → `windowSel`.  
Сломать `kind === "students" && …`.  
«С нуля» / «всю историю» — шаг 2.  
Текст готовности: «Касса на месте», не «и журнал».

## Шаг 5. Подсказки

`HINT.recheckOneMoney`:

> Синяя очередь справа на шаге 4. Окно — список в этой синей рамке (± месяц / ± три / ± шесть). Сначала касса — все страницы, не окно дат. Если касса за этот заход дочитана — одна перепись уроков за ±N (не с 2015), ушедшие в окне снять. Дырки не добирает (шаг 2). Вправо, когда страницы кассы дочитаны. Сумма с шапкой — шаг 5. Пауза 5 с. Стоп после текущего. В Alfa не пишет.

`HINT.loadOneMoney` — убрать ±1 ₽:

> Красная очередь слева, по одному, пауза 5 с. Только касса. Вправо, когда страницы дочитаны. Сумма с шапкой — шаг 5. Журнал не качает. Годы в этой рамке Alfa не режут. Синяя рамка не читается. В Alfa оплаты не создаёт.

`HINT.yearsMoney`: только вид красной рамки шага 4, как у шага 2. В кассу и в урок не идёт.

`HINT.recheckPay`: ± синей для одной переписи журнала; касса — все страницы.

`HINT.tabMoney`: красная — годы (экран), синяя — ± (в ядро).

Гид: касса без окна дат; синяя после кассы — перепись ±N, не добор. Клиентов не списком.

## Не делать

Ядро, шаг 5, лиды, деплой, checksum.

## Критерий D

1. Шаг 4 как 2 и 3: красная+годы, синяя+±, Стоп снаружи.
2. «± три» шага 4 не меняет шаг 2. Job: `recheckDays: 92`, `peopleKind: balance`.
3. Зелёная «Перепроверить» кассы показывает ±.
4. Нет подписи «Касса и журнал на месте».
5. `i` не обещает добор уроков и ±1 ₽.
6. `src/data/*` без диффа.

Простыми словами: шаг 4 выглядит как 2 и 3. Вправо по кассе. Синяя после кассы сверяет уроки за месяц, не добирает дырки.
