# ТЗ · чат D · шаг 4 экран

Себе, экран. Ядро — чат A. `src/data/*` не трогать. Шаг 5 не трогать.

Файл: `src/components/admin-crm-settings.tsx`.  
Не трогать: B, C, деплой (A).  
`BtnCluster` и `RecheckDaysSelect` уже есть — не копировать.

Годы красной ≠ окно синей. Checksum не показывать. Даты Alfa не считать.

## Закон

Шаг 4 **грузит кассу**. Рубли — шаг 5. Журнал — шаг 2.  
Экран Alfa не зовёт. Ядро: только `pay/index`. `recheckDays` и годы в урок не идут.

Не писать: «касса за ± месяц»; «сначала журнал»; «допишет занятия»; «±1 ₽»; «Касса и журнал на месте».

Текст зелёной: **«Касса на месте»**. Слева убрать «Касса и журнал — пока чего-то нет».

## Шаг 1. Ряд (как 2 и 3)

```
BtnCluster red:  [Загрузить по одному] [YearsSelect hint=yearsMoney]
BtnCluster sky:  [Перепроверить по одному] [RecheckDaysSelect]
[Стоп]
```

Не годы в синюю. Не ± в красную. Не «Медленный автодобор» / «Сверить счёт».

## Шаг 2. Свои списки

```
moneyRecheckDays: 32 | 92 | 182   // 32
moneyFromId: PEOPLE_FROM_OPTS     // "2015"
```

Не делить с шагом 2. Оба списка на шаге 4 **в Alfa не идут** (ядро грузит кассу без окна). Свои, чтобы «7 лет» / «± три» шага 2 не уезжали в job кассы.

## Шаг 3. Слот

Синяя: `recheckDays: moneyRecheckDays` можно слать — ядро проигнорирует.  
Красная: без `recheckDays`. `dateFrom` можно — ядро проигнорирует.

## Шаг 4. Карточка

Нет кассы → `years`. `full || dups` → `windowSel` (вид как 2 и 3).  
Сломать `kind === "students" && …`.  
«С нуля» — шаг 2.

## Подсказки

`HINT.recheckOneMoney`:

> Синяя очередь справа на шаге 4. Сначала касса — все страницы, все виды платежей по id, без окна дат. Курсор не сбрасывать. Вправо, когда страницы дочитаны. Дубли строк не держат очередь. Сумма с шапкой и списания — шаг 5. Журнал не качает. Список ± в этой рамке кассу не режет. Пауза 5 с. Стоп после текущего. В Alfa оплаты не создаёт.

`HINT.loadOneMoney`:

> Красная очередь слева, пауза 5 с. Только касса, все виды по id. Вправо, когда страницы дочитаны. Сумма — шаг 5. Журнал не качает. Годы Alfa не режут.

`HINT.yearsMoney` / `HINT.recheckPay`: вид как шаг 2 и 3; в кассу и в урок на шаге 4 не идут.

`HINT.tabMoney`: красная грузит, синяя перечитывает кассу. Рубли — шаг 5.

Гид: грузим платежи по id, не уроки, не остаток.

## Критерий D

1. Вид как 2 и 3: красная+годы, синяя+±, Стоп снаружи.
2. «± три» шага 4 не меняет шаг 2. Job: `peopleKind: balance`.
3. Нет «Касса и журнал на месте», нет ±1 ₽ в `i`.
4. `src/data/*` без диффа.

Простыми словами: шаг 4 выглядит как 2 и 3, а делает только загрузку кассы. Деньги — следующая вкладка.
