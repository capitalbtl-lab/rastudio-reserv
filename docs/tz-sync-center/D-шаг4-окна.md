# ТЗ · чат D · шаг 4 экран

Себе, экран. Ядро — чат A. `src/data/*` не трогать.

Файл: `src/components/admin-crm-settings.tsx`.
Не трогать: B, C, шаг 5, деплой (A).
`BtnCluster` и `RecheckDaysSelect` уже есть — не копировать.

Годы красной ≠ окно синей. Checksum не показывать. Даты Alfa на экране не считать.

## API (только `i`)

Экран Alfa не зовёт. Синяя шлёт `recheckDays`. Ядро само: `lesson/index` YYYY-MM-DD и status 1/2/3; `pay/index` без дат урока.

Не писать в `i`: «касса за ± месяц»; «сначала журнал»; «очередь ждёт уроки»; «90 секунд». Воркер шага 4 90 с не считает.

## Зачем

Шаги 2–3: красная рамка + свой список, синяя + ±.  
Шаг 4: две кнопки и один список лет. `peopleFromId` / `peopleRecheckDays` с шага 2.  
Карточка: `kind === "students" && (full || dups) ? windowSel : years` — у кассы всегда годы.

## Шаг 1. Ряд

```
BtnCluster red:  [Загрузить по одному] [YearsSelect hint=yearsMoney]
BtnCluster sky:  [Перепроверить по одному] [RecheckDaysSelect]
[Стоп]
```

Не годы в синюю. Не ± в красную. Не «Медленный автодобор» / «Сверить счёт».

## Шаг 2. Свои списки

```
moneyRecheckDays: 32 | 92 | 182   // 32
moneyFromId: PEOPLE_FROM_OPTS     // "2015"
```

Не делить с `peopleRecheckDays`, `groupsRecheckDays`, `peopleFromId`.

## Шаг 3. Слот

Синяя: `recheckDays: moneyRecheckDays`.  
Красная: `dateFrom: peopleDateFrom(moneyFromId)`, без `recheckDays`.  
Сегодня±N на экране не считать.

## Шаг 4. Карточка

`kind === "balance"`: нет кассы → `years`; `full || dups` → `windowSel`.  
Сломать `kind === "students" && …`.  
«С нуля» / «всю историю» — шаг 2.

## Шаг 5. Подсказки

`HINT.recheckWindow` — в обе стороны, не «только шаг 2».

`HINT.recheckOneMoney`:

> Синяя очередь справа на шаге 4. Окно — список в этой же синей рамке (± месяц / ± три / ± шесть), не годы из красной. Сначала касса — все страницы, не окно дат. Журнал окна — только если касса за этот заход дочитана, и только ±N, не с 2015. Вправо, когда страницы кассы дочитаны, даже если уроки не сошлись (дырка — шаг 2). Сумма с шапкой — шаг 5. Пауза 5 с. Стоп после текущего. В Alfa не пишет.

`HINT.loadOneMoney` — убрать «вправо по ±1 ₽»:

> Красная очередь слева, по одному, пауза 5 с. Сначала касса. Вправо, когда страницы дочитаны или касса уже была. Сумма с шапкой — шаг 5. Журнал — одна пачка и только если касса дочитана и дырка жива, годы из этой красной рамки, не «Добрать до конца». Платежи годы не режут. Синяя рамка не читается. В Alfa оплаты не создаёт.

`HINT.yearsMoney`: только красные шага 4 и «Загрузить кассу». Свой список. Режет календарь, если его ещё нет. Платежи нет.

`HINT.loadPay`: годы только для красной пачки уроков. Не обещать закрыть журнал.

`HINT.recheckPay`: ± синей для журнала; касса — все страницы, без дат урока.

`HINT.tabMoney`: красная — годы, синяя — ±.

Гид: касса без окна дат; журнал ±N только после дочитанной кассы. Клиентов не списком.

## Не делать

Ядро, `crm-pay.ts`, очередь, шаг 5, лиды, деплой, checksum, считать даты.

## Критерий D

1. Шаг 4 как 2 и 3: красная+годы, синяя+±, Стоп снаружи.
2. «± три» и «7 лет» шага 4 не меняют шаг 2. Job синей: `recheckDays: 92`, `peopleKind: balance`.
3. Зелёная «Перепроверить» кассы показывает ±.
4. `i`: не «вправо по ±1 ₽»; не «синяя сидит на уроках»; не «касса за ± месяц»; не «90 секунд».
5. `src/data/*` без диффа.

Простыми словами: шаг 4 выглядит как 2 и 3. Свои годы и свой ±. Вправо по кассе. Уроки — если касса уже дочитана, за ± месяц, не с 2015.
