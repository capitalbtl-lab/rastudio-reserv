# ТЗ · чат D · шаг 4 экран

Себе, экран. Ядро — чат A. `src/data/*` не трогать.

Файл: `src/components/admin-crm-settings.tsx`.
Не трогать: B, C, шаг 5, деплой (A).
`BtnCluster` и `RecheckDaysSelect` уже есть — не копировать.

Годы красной ≠ окно синей. Checksum не показывать. Даты Alfa на экране не считать.

## API (только текст `i`, запросов с экрана нет)

Синяя шлёт `recheckDays`. Ядро само ставит `lesson/index` `date_from`/`date_to` = YYYY-MM-DD, статусы 1/2/3.  
Касса — `pay/index` без дат урока (у pay в доке другой формат даты).  
Не писать в `i`, что касса режется ± месяцем. Не писать «сначала журнал, очередь ждёт уроки».

## Зачем

Шаги 2–3: красная рамка + свой список, синяя + ±.  
Шаг 4: две кнопки и один список лет. `peopleFromId` / `peopleRecheckDays` общие с шагом 2.  
Карточка: `kind === "students" && (full || dups) ? windowSel : years` — у кассы всегда годы.

## Шаг 1. Ряд

```
BtnCluster red:  [Загрузить по одному] [YearsSelect hint=yearsMoney]
BtnCluster sky:  [Перепроверить по одному] [RecheckDaysSelect]
[Стоп]
```

Не годы в синюю. Не ± в красную. Не «Медленный автодобор» / «Сверить счёт».

## Шаг 2. Свои списки

```
moneyRecheckDays: 32 | 92 | 182   // 32
moneyFromId: PEOPLE_FROM_OPTS     // "2015"
```

Не делить с `peopleRecheckDays`, `groupsRecheckDays`, `peopleFromId`.

## Шаг 3. Слот в ядро

Синяя: `recheckDays: moneyRecheckDays`. Не `peopleRecheckDays`.  
Красная: `dateFrom: peopleDateFrom(moneyFromId)`. Без `recheckDays`.  
Сегодня±N на экране не считать.

## Шаг 4. Карточка

`kind === "balance"`: нет кассы → `years` (`moneyFromId`); `full || dups` → `windowSel` (`moneyRecheckDays`).  
Сломать `kind === "students" && …`.  
«С нуля» / «всю историю» — только шаг 2.

## Шаг 5. Подсказки

`HINT.recheckWindow` — в обе стороны, не «только шаг 2».

`HINT.recheckOneMoney`:

> Синяя очередь справа на шаге 4. Окно — список в этой же синей рамке (± месяц / ± три / ± шесть), не годы из красной. Сначала касса этого человека — все страницы `pay/index`, не окно дат. Если успели — сверка журнала по номерам уроков в окне (новые дописать, ушедшие в окне снять). Вправо, когда страницы кассы дочитаны, даже если уроки не сошлись (дырка — шаг 2). Сумма с шапкой — шаг 5. Пауза 5 с. Стоп после текущего. В Alfa не пишет.

`HINT.loadOneMoney` — убрать «вправо по ±1 ₽»:

> Красная очередь слева, по одному, пауза 5 с. Сначала касса на диск. Вправо, когда страницы дочитаны или касса уже была. Сумма с шапкой — шаг 5. Если журнала нет и время осталось — одна пачка уроков по годам из этой красной рамки, не «Добрать до конца». Платежи годы не режут. Синяя рамка не читается. В Alfa оплаты не создаёт.

`HINT.yearsMoney`: только красные шага 4 и «Загрузить кассу». Свой список. Режет календарь, если его ещё нет. Платежи нет.

`HINT.loadPay`: окно лет только для красной пачки уроков. Не обещать закрыть журнал.

`HINT.recheckPay`: ± синей для журнала; касса — все страницы, без дат урока.

`HINT.tabMoney`: красная — годы, синяя — ±. Не «справа те же годы» на обе.

Гид: касса без окна дат, журнал синей ±N. Клиентов не списком.

## Не делать

Ядро, `crm-pay.ts`, очередь, шаг 5, лиды, деплой, checksum, считать даты, слать `pay/index` с экрана.

## Критерий D

1. Шаг 4 как 2 и 3: красная+годы, синяя+±, Стоп снаружи.
2. «± три» и «7 лет» шага 4 не меняют шаг 2. Job синей: `recheckDays: 92`, `peopleKind: balance`.
3. Зелёная «Перепроверить» кассы показывает ±.
4. `i`: не «вправо по ±1 ₽»; не «синяя сидит, пока уроки не сойдутся»; не «касса за ± месяц».
5. `src/data/*` без диффа.

Простыми словами: шаг 4 выглядит как 2 и 3. Свои годы и свой ±. Вправо по кассе. Журнал — если успели, очередь из-за него не встаёт.
