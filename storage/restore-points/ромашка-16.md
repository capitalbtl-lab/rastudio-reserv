# Ромашка-16

Точка восстановления **15 сентября 2026, 16:01 UTC**.

Полный снимок дерева rastudio.org: код, `public/` (включая media), scripts, docs, content, server, migrations, storage точек, конфиги.

- Тег: `romashka-16`
- Ветка отката: `restore/romashka-16`
- Код на момент снимка: `82aa759` (плюс коммит с `ROMASHKA_REV = 16`)
- Исходники: `storage/backups/ромашка-16-src.tar.gz`
- Полный архив: `storage/backups/ромашка-16-full.tar.gz` (1431 файлов, ~701 МБ)
- Предыдущая точка: ромашка 15 (`romashka-15` / `9246f7d`)

Не в tar: `node_modules`, `.output`, `.git` (собираются заново). Живые `crm-*.json` кабинета в этом клоне не лежали — их нет в снимке; они на VPS.

## Зачем эта точка

Откат всей системы целиком, если сломается История из Alfa, касса, журнал-job, витрина, media, скрипты выкладки.

Не откатывать 15 отдельно: 16 её включает.

## Как откатиться

```bash
git fetch origin
git checkout romashka-16
# или
git checkout restore/romashka-16

# исходники:
tar -xzf storage/backups/ромашка-16-src.tar.gz

# вся система с этого диска:
tar -xzf storage/backups/ромашка-16-full.tar.gz
```

Проверка: `src/data/romashka.ts` → `ROMASHKA_REV = 16`.

sha256 полного: `14ff34ce78b6587ff4332d33eef97e2e9c99654440e5f3824f3f03fa5a695542`
sha256 src: `6fae6d5808cedcc45ee372398751b7a8c7d50772a7616f37108de4619241e87f`

В Alfa из этого снимка ничего не улетает само.
