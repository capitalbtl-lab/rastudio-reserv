# Ромашка-14 — полный бэкап

Точка восстановления **12 сентября 2026, 14:11 UTC**.

Снимок живого кабинета rastudio.org **до** переписи ядра загрузчика «История из Alfa» (касса + склейка + complete).

- Живая ветка отката: `restore/romashka-14-live` (`cf1ff55`)
- Первый тег (не сдвинут): `romashka-14` / `restore/romashka-14` (`3d2a602`, 11 сентября)
- Архив исходников: `ромашка-14-src.tar.gz` (без `public/media`, без `node_modules` / `.output`)
- Полный архив: `ромашка-14-full.tar.gz` (код + `public` + манифесты, без `node_modules` / `.output` / `.git`)
- Архив первого тега: `ромашка-14-tag-src.tar.gz`
- Предыдущая точка: ромашка 13 (`romashka-13`)

`main` этим снимком не менялся. Автодеплой не запускался.

## Что внутри живого снимка (`cf1ff55`)

Всё из Ромашки-14: шаг 4 сверка по шапке Alfa (`customer.balance`), не по rest абонемента.
Плюс правки 12 сентября: один загрузчик, фон на диске, склейка без дублей, complete только при шапке ±1 ₽, касса слева снова читает Alfa, Баженов не пропускается из‑за fill.done.

## Чего нет

- Живой диск CRM на Beget (карточки, касса, журнал) в git не хранится (`storage/` в `.gitignore`). Этот бэкап — **код и медиа сайта**, не база учеников.
- Формы записи на сайте не входили в правки Ромашки-14 и не менялись этим снимком.
- Секреты `.env` не копировались.

## Как откатиться

```bash
git fetch origin
git checkout restore/romashka-14-live
# первый снимок шага 4 (11.09):
git checkout romashka-14

# только исходники:
tar -xzf storage/backups/ромашка-14-src.tar.gz

# полный снимок с public/media:
tar -xzf storage/backups/ромашка-14-full.tar.gz
```

Проверка: `src/data/romashka.ts` → `ROMASHKA_REV = 14`.

В Alfa из этого снимка ничего не улетает само.
