# Ромашка-17

Точка восстановления **18 сентября 2026, 22:01 UTC**.

Полный снимок дерева rastudio.org: код, `public/` (включая media), scripts, docs, content, server, migrations, ТЗ шагов 1–5, storage точек, конфиги.

- Тег: `romashka-17`
- Ветка отката: `restore/romashka-17`
- Код на момент снимка: `bf0e213` (плюс коммит с `ROMASHKA_REV = 17`)
- Исходники: `storage/backups/ромашка-17-src.tar.gz` (551 файл, ~2.2 МБ)
- Полный архив: `storage/backups/ромашка-17-full.tar.gz` (1336 файлов, ~681 МБ)
- Предыдущая точка: ромашка 16 (`romashka-16` / `7777330`)

Не в tar: `node_modules`, `.output`, `.git` (собираются заново). Живые `crm-*.json` кабинета в этом клоне не лежали — их нет в снимке; они на VPS.

## Зачем эта точка

Откат всей системы целиком после канонов 4–5, пульта синхронизации, сверки кассы (возврат вычитает, корректировки ±, товар в оплатах не в остатке) и ТЗ 1–5 (грязь / экраны / шаги 6–8).

Не откатывать 16 отдельно: 17 её включает (код main после 16).

## Как откатиться

```bash
git fetch origin
git checkout romashka-17
# или
git checkout restore/romashka-17

# исходники:
tar -xzf storage/backups/ромашка-17-src.tar.gz

# вся система с этого диска:
tar -xzf storage/backups/ромашка-17-full.tar.gz
```

Проверка: `src/data/romashka.ts` → `ROMASHKA_REV = 17`.

sha256 полного: `891901457b3baea8b9d97aa007162445aed39302860ebe3dd2d8b6ccdea758cb`
sha256 src: `116a354ec33ac9bd4b186e767159adadb8b28ea2906caae041fc03873f95f89e`

В Alfa из этого снимка ничего не улетает само.
