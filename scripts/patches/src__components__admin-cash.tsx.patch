diff --git a/src/components/admin-cash.tsx b/src/components/admin-cash.tsx
index e880e2d..ea4ad5e 100644
--- a/src/components/admin-cash.tsx
+++ b/src/components/admin-cash.tsx
@@ -55,8 +55,9 @@ function methodName(id?: string) {
   return ALFA_PAY_METHODS.find((x) => x.id === s)?.name || "—";
 }
 
-function rowSum(p: { kind?: string; income?: number; expenditure?: number }) {
-  if (p.kind === "product") return 0;
+function rowSum(p: { kind?: string; income?: number; expenditure?: number; refundOfGoods?: boolean }) {
+  if (p.kind === "product") return -(Math.abs(Number(p.expenditure || 0) || Number(p.income || 0)));
+  if (p.kind === "refund" && p.refundOfGoods) return Math.abs(Number(p.expenditure || 0) || Number(p.income || 0));
   return Number(p.income || 0) - Number(p.expenditure || 0);
 }
 
@@ -483,7 +484,7 @@ export function AdminCash({ active, onOpenClient }: { active?: boolean; onOpenCl
                   <td className="px-2 py-2">{p.branchName || CRM_BRANCH[p.branchId]?.short || p.branchId}</td>
                   <td className="px-2 py-2">{kindName(p.kind)}</td>
                   <td className={cn("px-2 py-2 whitespace-nowrap tabular-nums font-semibold", sum < 0 ? "text-rose-600" : "")}>
-                    {p.kind === "product" ? money(p.income) : `${sum > 0 ? "+" : ""}${money(sum)}`}
+                    {`${sum > 0 ? "+" : ""}${money(sum)}`}
                   </td>
                   <td className="px-2 py-2">{itemName(p.payItemId)}</td>
                   <td className="px-2 py-2">{methodName(p.payMethod)}</td>
@@ -506,7 +507,7 @@ export function AdminCash({ active, onOpenClient }: { active?: boolean; onOpenCl
                           parent: p.parent,
                           date: p.documentDate,
                           kind: kindName(p.kind),
-                          sum: money(p.kind === "product" ? p.income : sum),
+                          sum: money(sum),
                           article: itemName(p.payItemId),
                           method: methodName(p.payMethod),
                           cttId: p.cttId,
