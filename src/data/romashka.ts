/**
 * Точки восстановления кабинета rastudio.org.
 * Имя «ромашка N» — снимок архитектуры, к которому можно откатиться.
 *
 * ромашка 1 — кабинет, цены, расписание, AlfaCRM.
 * ромашка 2 — карта ID (customerId / groupId / branchId / courseId / card:*).
 * ромашка 3 — единая карточка клиента, занятия и абонемент из карточки,
 *             лиды/архив только по кнопке, липкий рабочий стол клиентов.
 * ромашка 4 — пропущена (заказ сразу «Ромашка-5»).
 * ромашка 5 — группы как в CRM (статусы, приоритет, состав-roster),
 *             витрина сайта, запись без iframe, роли ИИ консультант/админка.
 * ромашка 6 — мастер абонементов учеников: добавление / изменение / удаление
 *             раздельно, темп выборочно/быстро/медленно, без кэша, раздельный счёт.
 *
 * Полный бэкап: storage/backups/ромашка-6-full.tar.gz
 * Замороженная ветка: restore/romashka-6
 * Тег: romashka-6
 */
export const ROMASHKA_REV = 6;
export const ROMASHKA_NAME = "Ромашка-6";
export const ROMASHKA_ID = "romashka-6";
export const ROMASHKA_AT = "2026-09-05T00:08:00Z";

export const ROMASHKA_NOTE = [
  "Всё из Ромашки-5: карта ID, статусы групп CRM, приоритет, витрина сайта, запись без iframe, роли ИИ.",
  "Мастер абонементов учеников — три режима: добавление, изменение срока, удаление. Прогоны не делят список и кэш.",
  "Темп: выборочно / все быстро (школа за школой) / все медленно (пачки по 3 группы по номеру, сверка 3 раза, паузы вдвое, лог кто и когда).",
  "Назначение: абонемент пишется один раз. Сверка не дублирует. По умолчанию раздельный счёт (is_separate_balance).",
  "Удаление по одному человеку, без обрыва всего прогона. Видны все с живым абонементом CRM, в том числе лиды с выписанным.",
  "Живой абонемент = не снят и не истёк. Старт сегодня/завтра (только что назначили) — живой. Архив шаблона в CRM не снятие с карточки.",
  "Состав и абонементы мастер читает из CRM без кэша. Перезагрузить список — кнопка на шаге групп.",
  "Лиды без абонемента в изменении/удалении не входят. Добавление может брать всех привязанных, включая лидов.",
].join(" ");

export type RestorePoint = {
  id: string;
  name: string;
  rev: number;
  at: string;
  git: string;
  note: string;
};

export function currentRestorePoint(git = ""): RestorePoint {
  return {
    id: ROMASHKA_ID,
    name: ROMASHKA_NAME,
    rev: ROMASHKA_REV,
    at: ROMASHKA_AT,
    git,
    note: ROMASHKA_NOTE,
  };
}
