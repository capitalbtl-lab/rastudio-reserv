/**
 * Точки восстановления кабинета rastudio.org.
 * Имя «ромашка N» — снимок архитектуры, к которому можно откатиться.
 *
 * ромашка 1 — кабинет, цены, расписание, AlfaCRM.
 * ромашка 2 — карта ID (customerId / groupId / branchId / courseId / card:*).
 * ромашка 3 — единая карточка клиента, занятия и абонемент из карточки,
 *             лиды/архив только по кнопке, липкий рабочий стол клиентов.
 * ромашка 4 — пропущена (заказ сразу «Ромашка-5»).
 * ромашка 5 — группы как в CRM (статусы, приоритет, состав-roster),
 *             витрина сайта, запись без iframe, роли ИИ консультант/админка.
 * ромашка 6 — мастер абонементов учеников: добавление / изменение / удаление
 *             раздельно, темп выборочно/быстро/медленно, без кэша, раздельный счёт.
 * ромашка 7 — диск = правда, Alfa год в очереди; консультант: воронка, отработка
 *             по неделям, голос без эха, запись курса без телефона; сайт и кабинет
 *             один репозиторий (нарезка web/staff/editor — план, не переезд).
 *
 * Полный бэкап: storage/backups/ромашка-7-full.tar.gz
 * Замороженная ветка: restore/romashka-7
 * Тег: romashka-7
 */
export const ROMASHKA_REV = 7;
export const ROMASHKA_NAME = "Ромашка-7";
export const ROMASHKA_ID = "romashka-7";
export const ROMASHKA_AT = "2026-09-06T12:14:00Z";

export const ROMASHKA_NOTE = [
  "Всё из Ромашки-6: карта ID, статусы групп, витрина, мастер абонементов, роли ИИ.",
  "Диск сайта — источник правды. AlfaCRM догоняет очередью (delta, fold local, remap своих id). Уход с Alfa — разъём, не сейчас: с ней работать год.",
  "Консультант: развилка уже ходим / впервые. «Хочу записать ребёнка» после «уже ходим» — курс, не телефон. Узнанный клиент: пропуск и пауза отдельно, отработка — сначала неделя, три слота своего педагога, ещё три, другой педагог. Пробное вместо отработки не ставить.",
  "Голос: перебивание по interim, не слышать себя, повтор ответа, поле ввода всегда на месте и подмигивает, когда нужен текст.",
  "Окно агента: ROLE_FLAGS и BOOK_TYPE_FLAGS, каналы VK/MAX/Novofon, мозг storage/agent-brain.json. Карта ID — таблица (визуального графа нет). Обучение: примеры вручную, автозахват «скажи так» ещё нет.",
  "Сайт: SEO с Wix, видео без автозвука, выбор филиала в расписании. Кабинет и сайт — один репозиторий. Нарезка apps/web|staff|editor + packages/core|agent — план, файлы не разъехались.",
].join(" ");

export type RestorePoint = {
  id: string;
  name: string;
  rev: number;
  at: string;
  git: string;
  note: string;
};

export function currentRestorePoint(git = ""): RestorePoint {
  return {
    id: ROMASHKA_ID,
    name: ROMASHKA_NAME,
    rev: ROMASHKA_REV,
    at: ROMASHKA_AT,
    git,
    note: ROMASHKA_NOTE,
  };
}
