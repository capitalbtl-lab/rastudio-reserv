/**
 * Точки восстановления кабинета rastudio.org.
 * Имя «ромашка N» — снимок архитектуры, к которому можно откатиться.
 *
 * ромашка 1 — кабинет, цены, расписание, AlfaCRM.
 * ромашка 2 — карта ID (customerId / groupId / branchId / courseId / card:*).
 * ромашка 3 — единая карточка клиента, занятия и абонемент из карточки,
 *             лиды/архив только по кнопке, липкий рабочий стол клиентов.
 * ромашка 4 — пропущена (заказ сразу «Ромашка-5»).
 * ромашка 5 — группы как в CRM (статусы, приоритет, состав-roster),
 *             витрина сайта, запись без iframe, роли ИИ консультант/админка.
 * ромашка 6 — мастер абонементов учеников: добавление / изменение / удаление
 *             раздельно, темп выборочно/быстро/медленно, без кэша, раздельный счёт.
 * ромашка 7 — диск = правда, Alfa год в очереди; консультант: воронка, отработка
 *             по неделям, голос без эха, запись курса без телефона; сайт и кабинет
 *             один репозиторий (нарезка web/staff/editor — план, не переезд).
 * ромашка 8 — карточка клиента: живой абонемент, удаление как у группы, клик
 *             открывает карточку во вкладке Клиенты; Загрузить Клиентов/Лидов/Архив;
 *             выкладка без простоя (.output-next); сайт поднят после пустой сборки.
 * ромашка 9 — все 15 типов занятий как в Alfa; Ольга book_lesson по типу;
 *             пробное без group_ids, зал не слать если занят; календарь клиента
 *             с диска + inbound из Alfa; педагоги привязаны к группам.
 * ромашка 10 — карточка с диска без ожидания Alfa; касса: автоопрос 15 мин,
 *              окно 3 дня, все типы (доход, продажи, возвраты, корректировки);
 *              платежи по счетам абонементов; списания из отметок педагога;
 *              остаток = оплаты − списания; очередь pay/customer/lesson сразу.
 * ромашка 12 — два педагога на группе, сверка экспорта шаблона, мультивыбор.
 * ромашка 13 — История из Alfa шаги 1–3 (календарь / группы / касса):
 *              красная «Загрузить по одному» пауза 5 с, перепроверка: месяц 2 с / 3 мес 3 с / 6 мес 4 с, годы, «Перепроверить
 *              по одному», архив групп учеников, автодеплой. До внедрения
 *              ТЗ «Центр связи с AlfaCRM» этапы 1–8.
 * ромашка 14 — шаг 4 сверка остатка: эталон = общий остаток шапки Alfa
 *              (customer.balance), не rest абонемента; «совпало» не держит
 *              слева из‑за будущих уроков с ценой; касса/явка чинятся только
 *              если касса ≠ шапка. Формы записи на сайте не трогали.
 * ромашка 15 — История из Alfa: очередь rastudio-history, красная по одному
 *              пауза 5 с, синяя перепроверка не выкидывает влево, пока журнал
 *              на диске; «Сейчас ходят» = состав живых групп; касса 10/час;
 *              журнал-job на диске, вкладка только смотрит; сборка отвязана
 *              от вотчера.
 *
 * Полный бэкап: storage/backups/ромашка-15-src.tar.gz
 * Замороженная ветка: restore/romashka-15
 * Тег: romashka-15
 * Предыдущая: ромашка 14 (`romashka-14`)
 */
export const ROMASHKA_REV = 15;
export const ROMASHKA_NAME = "Ромашка-15";
export const ROMASHKA_ID = "romashka-15";
export const ROMASHKA_AT = "2026-09-13T00:17:00Z";

export const ROMASHKA_NOTE = [
  "Всё из Ромашки-14: сверка остатка по шапке Alfa (customer.balance), не по rest абонемента.",
  "История из Alfa: один загрузчик rastudio-history; красная «по одному» пауза 5 с; синяя не выкидывает влево, пока журнал на диске.",
  "«Сейчас ходят» — состав живых групп админки, не все is_study=1.",
  "Касса: автоопрос 10/час, date_from yyyy.mm.dd, cttId на диске.",
  "Журнал: job на диске, вкладка только смотрит. Сборка отвязана от вотчера.",
].join(" ");

export type RestorePoint = {
  id: string;
  name: string;
  rev: number;
  at: string;
  git: string;
  note: string;
};

export function currentRestorePoint(git = ""): RestorePoint {
  return {
    id: ROMASHKA_ID,
    name: ROMASHKA_NAME,
    rev: ROMASHKA_REV,
    at: ROMASHKA_AT,
    git,
    note: ROMASHKA_NOTE,
  };
}
