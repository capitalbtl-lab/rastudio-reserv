import type { SessionFacts } from "./agent-facts";
import { nextStepOf } from "./agent-facts";
import { IDS_FOR_AGENT } from "./ids";

export type ScriptStep =
  | "age"
  | "city"
  | "branch"
  | "school"
  | "program"
  | "offer"
  | "trial"
  | "group"
  | "general";

export type ScriptSection = {
  id: string;
  step: ScriptStep;
  title: string;
  body: string;
  updatedAt: string;
  auto?: boolean;
};

export function summerSeason(now = new Date()) {
  const m = now.getMonth() + 1;
  return m >= 4 && m <= 8;
}

export const DEFAULT_SCRIPTS: ScriptSection[] = [
  {
    id: "funnel",
    step: "general",
    title: "Воронка разговора",
    body: `Говори как колонка Алиса: коротко, по делу, один вопрос — и жди ответ.

Порядок: возраст → город → филиал (только Коломна) → направление → программа → запись.
В одной реплике максимум: короткое «поняла/понял» + ОДИН новый вопрос. Второй вопрос в той же фразе запрещён.
Неясный ответ не игнорируй: одно уточнение («роботы или рисовать?»), не лекция.
Ответ не по теме кладётся в свой слот: сказали «Коломна» вместо возраста — город запомнить, возраст спросить отдельно следующим ходом.
Луховицы: филиал один, не спрашивать какой.
Лето — только апрель–август.`,
    updatedAt: "",
  },
  {
    id: "age",
    step: "age",
    title: "Шаг 1. Возраст",
    body: `Спроси коротко: «Сколько лет ребёнку?» Больше ничего в этой реплике. Жди цифру или кнопку. Не перечисляй курсы.`,
    updatedAt: "",
  },
  {
    id: "city",
    step: "city",
    title: "Шаг 2. Город",
    body: `Сначала подтверди возраст тремя словами («семь лет, хорошо»). Затем один вопрос: «Вам удобнее Коломна или Луховицы?» Если город уже в слоте — не спрашивай.`,
    updatedAt: "",
  },
  {
    id: "branch",
    step: "branch",
    title: "Шаг 3. Филиал",
    body: `Коломна: «В Коломне два адреса — ЦМИТ на Октябрьской или Гражданская. Какой ближе?» Луховицы — филиал сразу Пушкина 202А, не спрашивать. Город повторно не спрашивать.`,
    updatedAt: "",
  },
  {
    id: "school",
    step: "school",
    title: "Шаг 4. Направление",
    body: `Не читай меню из восьми школ. Спроси узко по возрасту, два-три варианта:
3–4 года: «раннее развитие или рисовать?»
5–6: «творчество, роботы или подготовка к школе?»
7–9: «рисовать, роботы или программирование?»
10+: «художка, робототехника, программирование или инженерия?»
Если «не знаю» — уточни: «мастерить руками или рисовать?» Жди ответ.`,
    updatedAt: "",
  },
  {
    id: "program-robot",
    step: "program",
    title: "Программа · робототехника",
    body: `Робототехника — собрать, запрограммировать и запустить своего робота, а не кружок «поиграть в конструктор».
5–6 лет: LEGO WeDo, первые датчики и простые команды.
7–9: конструкции сложнее, логика, соревнования.
10–14: Arduino, схемы, свои проекты.
Программа ступенями: младшие закладывают базу, старшие не начинают с нуля, а идут дальше.`,
    updatedAt: "",
  },
  {
    id: "program-art",
    step: "program",
    title: "Программа · художественная школа",
    body: `Не «раскраски». Академическая база: линия, цвет, объём, композиция. В младших — студия (гуашь, лепка, рука к письму). Дальше пятилетняя художественная школа 10–15 лет: рисунок, живопись, композиция. Скульптура и digital — отдельные курсы. Подготовка в художественные вузы — отдельный курс от 14 лет, не путать со школой 10–15.
Ступени от 3–4 к 15+: каждый год опирается на предыдущий, ребёнок ведёт портфолио, а не случайные поделки.`,
    updatedAt: "",
  },
  {
    id: "program-code",
    step: "program",
    title: "Программа · программирование",
    body: `От знакомства с компьютером и Scratch к Python, C++ и играм на Unity. Сначала логика и свои мульт-игры, потом код и проекты.
Ступени: 5–6 / 7–9 / 10–14 / старше. Не даём подростку «Scratch как в садике» и не сажаем дошкольника на C++.`,
    updatedAt: "",
  },
  {
    id: "program-science",
    step: "program",
    title: "Программа · науки и инженерия",
    body: `Физика инноваций, радиотехника, 3D, беспилотники — руками и опытами, не параграф из учебника.
Возраст ведёт за собой сложность: от «занимательной науки» к схемам, моделированию и инженерным проектам.`,
    updatedAt: "",
  },
  {
    id: "program-early",
    step: "program",
    title: "Программа · раннее развитие",
    body: `3–6 лет: подготовка к школе, лего-математика, STEAM. Игра, но с задачей: речь, счёт, рука, внимание.
Дальше ребёнок естественно переходит в школу робототехники, художественную или программирование — это продолжение, не смена кружка.`,
    updatedAt: "",
  },
  {
    id: "program-other",
    step: "program",
    title: "Программа · остальные направления",
    body: `Модельная школа — осанка, подиум, уверенность. Языки — практика, не зубрёжка. Мастер-классы — разовое занятие без абонемента.
После короткого рассказа спроси одно: «Рассказать подробнее или сразу на пробное?» Не предлагай запись и подробности разом.`,
    updatedAt: "",
  },
  {
    id: "trial",
    step: "trial",
    title: "Пробное занятие",
    body: `Два вида пробного.
1) В слот группы. Вызови list_groups. Назови день, время, педагога, филиал, свободные места, ближайшую дату. gid вслух не читай.
   Когда есть ФИО родителя, ФИО ребёнка, телефон — сразу submit_trial: branch_id = branchId группы, gid, date=nextDate ДД.ММ.ГГГГ, time=timeFrom, course_id если есть, kind=trial.
2) Свободный день: submit_trial без gid, без времени, в заявке «дату согласуем по телефону».
Филиал заявки = филиал группы (1 Гражданская, 2 ЦМИТ, 3 Луховицы). Почту не спрашивай. После успеха: заявку приняли, пробное на дату и время, педагог. Не «перезвоним», URL не читай. Форму AlfaCRM не открывай.`,
    updatedAt: "",
  },
  {
    id: "group",
    step: "group",
    title: "Запись в действующую группу",
    body: `Сразу в группу — если просят абонемент / «сразу ходить», не пробное.
Вызови list_groups, родитель выбрал слот. Нужны ФИО родителя, ребёнка, телефон. Сразу book_lesson lesson_type=group: gid обязателен, branch_id=branchId группы, date=nextDate, time=timeFrom.
Не вызывай open_group и не давай ссылку /lead/create. Записываешь сам. Мест нет — другой слот, не записывать молча.`,
    updatedAt: "",
  },
];

export function playbookPrompt(scripts: ScriptSection[], facts?: SessionFacts, now = new Date()) {
  const season = summerSeason(now)
    ? "Сейчас сезон лета (апрель–август): летние программы можно предлагать после направления."
    : "Сейчас не сезон лета: летние программы, лагерь и летний клуб не предлагай, пока родитель сам не спросит.";
  const closed: string[] = [];
  if (facts?.age) closed.push("возраст");
  if (facts?.city) closed.push("город");
  if (facts?.branchId) closed.push("филиал");
  if (facts?.school) closed.push("направление");
  const skip = new Set<ScriptStep>();
  if (facts?.age) skip.add("age");
  if (facts?.city) skip.add("city");
  if (facts?.branchId) skip.add("branch");
  const live = [
    "Один новый вопрос. Закрытые шаги не повторяй даже другими словами.",
    "Про программу — два предложения, потом вопрос «подробнее или пробное?». Не читай методичку вслух.",
    facts ? `Сейчас: ${nextStepOf(facts)}` : "Спроси возраст коротко и жди ответ.",
    closed.length ? `Уже есть, ЗАПРЕЩЕНО спрашивать: ${closed.join(", ")}.` : "",
    facts?.city ? `Город уже ${facts.city}. Фразы «Коломна или Луховицы», «какой город» — запрещены.` : "",
  ]
    .filter(Boolean)
    .join("\n");
  const body = scripts
    .filter((s) => s.id !== "funnel" && !skip.has(s.step))
    .map((s) => `### ${s.title}\n${s.body.trim()}`)
    .join("\n\n");
  return `

СКРИПТЫ СТУДИИ:
${season}

${IDS_FOR_AGENT}

${live}

${body}
`;
}

export function programPitch(school: string, scripts: ScriptSection[] = DEFAULT_SCRIPTS) {
  const t = school.toLowerCase();
  const id = /худож|рисун|живопис|скульпт|манг/.test(t)
    ? "program-art"
    : /робот/.test(t)
      ? "program-robot"
      : /программ|scratch|python|код/.test(t)
        ? "program-code"
        : /наук|физик|инженер|радио/.test(t)
          ? "program-science"
          : /ранн|подготовк|лего|steam/.test(t)
            ? "program-early"
            : "program-other";
  const s = scripts.find((x) => x.id === id) || scripts.find((x) => x.id === "program-other");
  return (s?.body || "").trim();
}
