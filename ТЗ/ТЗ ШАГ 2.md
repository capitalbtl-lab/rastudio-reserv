ТЗ ТЗ-2. Экраны. Не шаг 2 Истории (календарь). Календарь не трогать. Не внедрять, пока не принят ТЗ-1 и не скажут «делай ТЗ-2».

0. Зависимость от ТЗ-1 (построчно)

1. Носитель dirty уже есть: один словарь на customerId, поля is_study, removed, crm_current_branches, live_tariff, lead_status_id. Ключ только customerId.
2. ТЗ-2 не меняет markDirty / clearDirty / canInboundWrite. Только читает диск значений extras и groupLinks.
3. Чип и список смотрят значение на диске, не Alfa. Dirty поле всё равно закон экрана: Ольга уже записала — чип это видит. Бейдж dirty не делать.
4. ТЗ-2 не вызывает inbound 6–8 и не обходит canInboundWrite.
5. ТЗ-2 не ставит dirty (нет записи extras роли/тарифа/воронки).
6. Слот: второй dirty не заводить; приоритет/лимит как в слоте сейчас.
7. groupLinks и касса не в словаре ТЗ-1 — занято считается по связям как есть.
8. Код шагов 1–5 Истории не в diff. Стоп-файлы те же, что в ТЗ-1. personRole для сверки (dossierAuditRole) не трогать. Для чипов Клиентов — существующий personRole как на вкладке сейчас; под removed=2 его в ТЗ-2 не переучивать (чип Архив от набора, не от роли).
9. Нет приёмки ТЗ-1 (8 пунктов) — ТЗ-2 не начинать.

1. Цель

Один котёл на чип. Не подгружать Alfa с вкладки. Не второй счётчик.

2. Вкладка «Клиенты» — числа

Котёл статуса = досье; скрыть removed=1; скрыть personRole ≠ выбранный чип (для Текущие/Лиды).

| Чип | Формула | Не формула |
| --- | --- | --- |
| Текущие | count personRole = учится (is_study=1) | не rows.length, не сумма мест групп, не живая Alfa, не crm_current как закон, не занято групп |
| Лиды | personRole = лид | не воронка важнее is_study=1 (шаг 5 Истории не трогать) |
| Архив | id ∈ набор шага 2 Истории (working). Нет набора → 0 и текст «набор пуст» | не все removed=2; не «и роль архив»; модуль 6 может наставить removed — чип не обязан вырасти |
| Дети | то же число, что активный чип статуса | не копия «Текущие» при открытых Лидах |
| Филиал «Все» | то же, что активный чип статуса (уникальные люди) | не сумма четырёх филиалов |
| Филиал 1..4 | человек в чипе, если branchId или crm_current_branches или живой groupLink этого филиала | один человек на двух площадках — в двух чипах, в «Все» один раз |
| Абонемент «Все» | длина котла статуса (как Текущие/Лиды/Архив сейчас) | не страница списка, не 240 при 1843, не pupil-tariffs.length |
| С | в котле live_tariff === "1" на диске (даже если dirty) | не pupil-tariffs.length (это ТЗ-4) |
| Без | Все − С | |

Список слева может быть 128 из N. В поиске: показано / N чипа. N = чип статуса.

Фильтр филиала: человек проходит, если попал в формулу этого филиала (не только домашний branchId). Это чтение досье, не шаг 1 Истории.

3. Вкладка «Группы» — места

| Факт | Формула |
| --- | --- |
| Занято | живые groupLinks этой пары branchId+groupId (ученик и лид) |
| Лимит | слот/карточка limit |
| Свободно | лимит − занято, только если лимит задан. Нет поля / не число — прочерк, не ∞ и не путать с «мест 0» |
| limit === 0 | задан ноль мест, свободно 0, не «не задан» |
| Приоритет, сетка | слот, как сейчас |
| Журнал | не счётчик мест |

Подписи разные: не писать занято в чип «Текущие».

4. Чего не писать на диск в ТЗ-2

Ни is_study, ни live_tariff, ни набор архива, ни groupLinks, ни lead_status_id, ни crm_current_branches. Нет новых полей. Нет вызова markDirty.

5. Файлы

Чтение: searchClientViews / чипы admin-clients (котёл, филиалы, тариф). Счёт мест: takenMapFromLinks, UI группы/слота.

Не править: inbound 1–5, модуль dirty ТЗ-1, crm-person-role (сверка и роль архива), dossierAuditRole.

6. Обязательная проверка ТЗ-2

1. ТЗ-1 зелёный, хелперы dirty на месте.
2. Филиалы: сумма 1+2+3+4 ≥ «Все»; «Все» = числу Текущие (при чипе Текущие).
3. Человек с двумя ветками: в двух филиалах, в «Все» один раз.
4. Абонемент «Все» = Текущие (на Текущих), не длина rows.
5. live_tariff=1 dirty (через хелпер ТЗ-1) → чип «С» включает этого id.
6. Архив без набора = 0 + «набор пуст». removed=2 вне набора не увеличивает чип.
7. Группа: занято = count связей; лид в группе +1 к занято, 0 к Текущим.
8. Лимит отсутствует → нет «свободно N»; лимит 0 → свободно 0.
9. Grep: ТЗ-2 не вызывает markDirty, не импортирует inbound шага 1 cgi write, не меняет personRole/сверку.
10. Контрольный id шага 4–5 Истории: А и шапка не в diff.
11. Typecheck.
12. Поиск: показано/N, N = чип, не cap.

Нет всех 12 — ТЗ-3 не начинать.

7. Приёмка одной фразой

Чипы и места лгут только если диск лжёт; грязный тариф уже на диске в «С»; Архив = набор; шаги 1–5 и словарь ТЗ-1 не переписаны.

Пишите «делай ТЗ-2» — только экраны.
