ТЗ ТЗ-2. Экраны. Не шаг 2 Истории (календарь). Календарь не трогать. Не внедрять, пока не принят ТЗ-1 и не скажут «делай ТЗ-2».

0. Зависимость от ТЗ-1 (построчно)

1. Носитель dirty уже есть: один словарь на customerId, поля is_study, removed, crm_current_branches, live_tariff, lead_status_id. Ключ только customerId.
2. ТЗ-2 не меняет markDirty / clearDirty / canInboundWrite. Только читает диск extras и groupLinks.
3. Чип и список смотрят значение на диске, не живую Alfa. Грязное поле всё равно закон экрана: оператор уже записал — чип это видит. Бейдж dirty не делать.
4. ТЗ-2 не вызывает inbound 6–8 и не обходит canInboundWrite.
5. ТЗ-2 не ставит dirty (нет записи extras роли/тарифа/воронки/веток).
6. Слот: второй dirty не заводить; приоритет/лимит как в слоте сейчас.
7. groupLinks и касса не в словаре ТЗ-1 — занято считается по связям как есть.
8. Код шагов 1–5 Истории не в diff. Стоп-файлы те же, что в ТЗ-1. dossierAuditRole сверки не трогать. Чип вкладки «Клиенты» = то, что уже рисует `viewOf` → `personRole(...)` (не сырой `d.status` в обход personRole). Под removed=2 personRole в ТЗ-2 не переучивать: чип Архив от набора шага 2, не от «все removed=2».
9. Нет приёмки ТЗ-1 (9 пунктов) — ТЗ-2 не начинать.

1. Цель

Один котёл на чип. Не подгружать Alfa с вкладки. Не второй счётчик. Формулы совпадают с уже живым `searchClientViews` / `viewOf`, если экран уже так считает — не ломать ради новых имён.

2. Вкладка «Клиенты» — числа

Котёл = `viewOf` по досье. Скрыть `personRole === "удалён"` (сейчас это extras.removed == 1 **как значение строки**, не фильтр index `removed:1` = «активные и архивные»). Скрыть personRole ≠ выбранный чип (для Текущие/Лиды).

Роль на чипе: тот же `personRole`, что `viewOf` уже зовёт. is_study=1 → учится (хвост воронки ученика лидом не делает). is_study=0 → лид. is_study=2 на старом диске → архив в personRole, в чип «Текущие» не входит. Новую двойку inbound не пишет (ТЗ-1/3).

| Чип | Формула (как searchClientViews сейчас, не новый счётчик) | Не формула |
| --- | --- | --- |
| Текущие | count personRole = «учится» | не rows.length, не сумма мест групп, не живая Alfa, не crm_current как закон, не занято групп, не lead_status_id |
| Лиды | personRole = «лид» | не воронка важнее is_study=1 (шаг 5 Истории / dossierAuditRole не трогать) |
| Архив | id ∈ набор шага 2 Истории (working) и personRole архив. Нет набора → 0 и текст «набор пуст» | не все extras.removed=2; модуль 6 не надувает этот чип |
| Дети | то же число, что активный чип статуса | не копия «Текущие» при открытых Лидах |
| Филиал «Все» | то же, что активный чип статуса (уникальные люди) | не сумма четырёх филиалов |
| Филиал 1..4 | человек в чипе, если домашний branchId **или** crm_current_branches **или** живой groupLink этого филиала (уже `viewOf.branchIds`) | один человек на двух площадках — в двух чипах, в «Все» один раз |
| Абонемент «Все» | длина котла статуса | не страница списка, не pupil-tariffs.length |
| С | в котле extras.live_tariff === "1" (даже если dirty) | не length строк; предикат «живой» задаёт ТЗ-4 = `tariffRowLive` |
| Без | Все − С | |

crm_current_branches после ТЗ-3 = `branch_ids` карточки. Пустой массив с Alfa (ТЗ-3) затирает **это поле**; чип филиала всё ещё может держать человека через groupLink и branchId. Это не баг ТЗ-2.

«Все» уникальные; сумма 1+2+3+4 может быть больше «Все».

Список слева может быть 128 из N. В поиске: показано / N чипа. N = чип статуса.

Фильтр филиала: человек проходит, если попал в `branchIds` этого филиала. Чтение досье, не шаг 1 Истории.

3. Вкладка «Группы» — места

| Факт | Формула |
| --- | --- |
| Занято | живые groupLinks этой пары branchId+groupId (ученик и лид) |
| Лимит | слот/карточка limit |
| Свободно | лимит − занято, только если лимит задан. Нет поля / не число — прочерк, не ∞ и не путать с «мест 0» |
| limit === 0 | задан ноль мест, свободно 0, не «не задан» |
| Приоритет, сетка | слот, как сейчас |
| Журнал | не счётчик мест |

Подписи разные: не писать занято в чип «Текущие».

4. Чего не писать на диск в ТЗ-2

Ни is_study, ни live_tariff, ни набор архива, ни groupLinks, ни lead_status_id, ни crm_current_branches. Нет новых полей. Нет вызова markDirty. Нет запросов в Alfa. Не менять personRole / dossierAuditRole / tariffRowLive.

5. Файлы

Чтение: searchClientViews / чипы admin-clients (котёл, филиалы, тариф). Счёт мест: takenMapFromLinks, UI группы/слота.

Не править: inbound 1–5, модуль dirty ТЗ-1, crm-person-role (сверка и роль архива), dossierAuditRole, crm-tariff-row.

Если формулы уже совпадают — diff экрана может быть пустым. Пустой diff при зелёных 12 проверках = приёмка ТЗ-2, не провал.

6. Обязательная проверка ТЗ-2

1. ТЗ-1 зелёный, хелперы dirty на месте.
2. Филиалы: сумма 1+2+3+4 ≥ «Все»; «Все» = числу Текущие (при чипе Текущие).
3. Человек с двумя ветками: в двух филиалах, в «Все» один раз.
4. Абонемент «Все» = Текущие (на Текущих), не длина rows.
5. live_tariff=1 dirty (через хелпер ТЗ-1) → чип «С» включает этого id.
6. Архив без набора = 0 + «набор пуст». removed=2 вне набора не увеличивает чип.
7. Группа: занято = count связей; лид в группе +1 к занято, 0 к Текущим.
8. Лимит отсутствует → нет «свободно N»; лимит 0 → свободно 0.
9. Grep: ТЗ-2 не вызывает markDirty, не импортирует inbound шага 1 cgi write, не меняет personRole/сверку/tariffRowLive, не ходит в v2api.
10. Контрольный id шага 4–5 Истории: А и шапка не в diff.
11. Typecheck.
12. Поиск: показано/N, N = чип, не cap.

Нет всех 12 — ТЗ-3 не начинать.

7. Приёмка одной фразой

Чипы и места лгут только если диск лжёт; грязный тариф уже на диске в «С»; Архив = набор; personRole/сверка/1–5 и словарь ТЗ-1 не переписаны.

Пишите «делай ТЗ-2» — только экраны (или пустой diff, если уже так).
