ТЗ ТЗ-1. Грязь. Это не шаг 1 Истории (состав). Состав не трогать. Не внедрять, пока не скажут «делай ТЗ-1».

0. Рамка

Цель: уметь пометить поле человека «на диске наше, Alfa ещё не подтвердила», чтобы модули 6–8 не затёрли день.

Не цель: научить шаги 1–5 Истории пропускать dirty. If dirty в cgi/журнале/кассе/шапке — запрещено.

Ключ записи словаря: только `customerId`. Не ФИО. Не `branchId+groupId`. Слот в этот словарь не входит.

Чужой файл Ольги — не писать, сказать.

1. Какие поля

Только те, что потом пишут модули 6–8:

| Поле | Где | Смысл |
| --- | --- | --- |
| is_study | extras досье | роль |
| removed | extras | архив Alfa / удалён |
| crm_current_branches | extras | филиалы последнего прогона 6 |
| live_tariff | extras | чип абонемента |
| lead_status_id | extras | воронка |

Не dirty в этом шаге: строки кассы, lessonId, groupLinks, ФИО, слот.

Слот: не заводить второй флаг. Уже есть «не выгружен в CRM» — оставить. В коде ТЗ-1 слот не ломать и не дублировать.

is_study=2 в карточку не писать (ни этот шаг, ни 6–8). removed=1 не в чипах (чипы — ТЗ-2).

2. Форма штампа на диске

Рядом с досье, не второй мозг роли. Один словарь на customerId:

- fields: имена из таблицы, которые грязные
- at: ISO, когда поставили dirty (на поле или на запись — как удобно, но снятие по полю)
- source: site | olga | queue
- ackedAt на поле — когда сняли

Нет поля в словаре = чистое. История 1–5 этот словарь не читает (их код не трогаем). Читатели: хелпер для ТЗ-3…5 и очередь, если уже пишет эти поля.

Один носитель, не два: модуль вроде crm-field-dirty.ts + запись в существующий sync JSON по customerId (рядом со штампами А, не вторая карта людей). Не раздувать dossiers.json второй картой ID. Если всё же extras — одна строка JSON, не status2.

Экраны в ТЗ-1 бейдж не обязаны.

3. Контракт записи по полям (для ТЗ-3…5)

Общее: canInboundWrite(customerId, field) = false, если поле в dirty.

Исключение только для тарифа (ТЗ-4): dirty live_tariff → не писать ни live_tariff, ни строки pupil-tariffs этого id (атомарно). Иначе чип ТЗ-2 разъедется со строками.

Для is_study / removed / crm_current_branches / lead_status_id: dirty одно поле — другие чистые поля того же id писать можно.

crm_current_branches — одно поле целиком. Не «чужой филиал внутри поля». Dirty → не писать ветки вообще.

4. Когда ставить dirty

Обязательны markDirty(customerId, field, source) и clearDirty(customerId, field) после ack.

Ставить, если уже есть запись с сайта/очереди в эти extras (поиск customer.update / смена роли / тариф / этап в этом чате D). Нашли писателя в разрешённых файлах — обернуть markDirty до очереди в Alfa. Файл Ольги (E) — не править, сказать человеку. Не нашли писателя — хелпер есть, вызов повесят ТЗ-3…5. Молча не плодить второй writer extras.

Не ставить dirty от загрузки Alfa и от шага 1 Истории.

5. Когда снимать

Только ack: очередь вернула успех по этому полю (customer.update / customer-tariff / lead-status).

Пустой ответ / 429 / сеть — dirty жив. Счётчик skip inbound не путать с ack.

Не снимать по таймауту. Не снимать, потому что пришла ночь 1–5. Inbound 6–8 не вызывает clearDirty.

6. Поведение загрузчика 6–8 (контракт, код 6 ещё нет)

canInboundWrite(customerId, field): false если поле в dirty.

ТЗ-1 кладёт хелпер + тест. Модули 6–8 в этом шаге не пишутся. Заглушка теста вызывает хелпер.

Лог одной строки: customerId, поле, skip dirty. Счётчик: число skip с процесса (файл или память синка, не второй журнал людей). Экран пульта не обязан в ТЗ-1.

7. Файлы

Не трогать: inbound шагов 1–5, crm-balance-audit формулу, pipe пульта 1–5.

8. Обязательная проверка ТЗ-1

1. Unit: markDirty → canInboundWrite false; clearDirty → true.
2. Unit: dirty is_study не блокирует live_tariff (поле чистое).
3. Unit: неизвестный customerId без записи = все поля чистые.
4. Нет вызова хелпера из кода шага 1–5 (grep).
5. Слот: grep не добавил второй dirty приоритета в досье / в этот словарь.
6. Контрольный живой id шага 4–5 Истории: А и шапка те же (код пути кассы не в diff).
7. Typecheck.
8. Если обернули существующий export роли/тарифа в разрешённых файлах — тест: успешный mock ack снимает dirty; ошибка оставляет.

Нет всех восьми — ТЗ-2 не начинать.

9. Приёмка одной фразой

На диске у customerId можно испачкать роль/ветки/тариф/воронку; загрузка этих полей (хелпер) не затирает; шаги 1–5 в diff не входят; касса контрольного id не едет.

Пишите «делай ТЗ-1» — только этот пункт.
